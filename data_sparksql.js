/* data_sparksql.js — Spark SQL Data Engineering Concepts
   Santosh Jammi | Principal Data Architect
   Coverage: Beginner → Intermediate → Advanced → Architect
*/
window.SPARKSQL_DATA = [
  {
    "id": "beg_001",
    "title": "spark.sql() Basics – Running SQL in PySpark",
    "level": "beginner",
    "category": "Spark SQL Fundamentals",
    "description": "spark.sql() allows you to execute SQL statements directly against the Spark session. It returns a DataFrame that can be further transformed or displayed. This is the entry point for all SQL-based interactions in Databricks notebooks.",
    "code": "-- In a Databricks notebook cell (SQL mode)\n-- Or via spark.sql() in PySpark:\n\n-- 1. Simple query against an in-memory view\nSELECT current_date() AS run_date, current_user() AS executed_by;\n\n-- 2. Create a temp view from a DataFrame (PySpark interop)\n-- df = spark.read.format('delta').load('/mnt/bronze/transactions')\n-- df.createOrReplaceTempView('raw_transactions')\n\n-- 3. Query the temp view via SQL\nSELECT\n    txn_id,\n    account_id,\n    txn_amount,\n    txn_currency,\n    txn_date\nFROM raw_transactions\nWHERE txn_amount > 10000\n  AND txn_currency = 'EUR'\nLIMIT 100;\n\n-- 4. Set Spark SQL config within SQL\nSET spark.sql.shuffle.partitions = 200;\nSET spark.databricks.delta.optimizeWrite.enabled = true;\n\n-- 5. Use spark.sql() result as DataFrame (PySpark)\n-- result_df = spark.sql(\"\"\"\n--     SELECT account_id, SUM(txn_amount) AS total_amount\n--     FROM raw_transactions\n--     GROUP BY account_id\n-- \"\"\")\n-- result_df.display()",
    "notes": [
      "spark.sql() returns a DataFrame — chain .display(), .show(), .write, etc.",
      "SQL cells in Databricks use %sql magic — no need to wrap in spark.sql()",
      "Temp views created via createOrReplaceTempView are session-scoped; Global temp views persist across sessions within the same app",
      "SET commands configure Spark session properties and can affect query performance significantly",
      "In Unity Catalog environments, always qualify table names: catalog.schema.table"
    ],
    "use_case": "A TCS BFSI team running ad-hoc regulatory queries on Azure Databricks — analysts write SQL while engineers orchestrate via PySpark pipelines, using spark.sql() to bridge both worlds in multi-hop Medallion Architecture notebooks."
  },
  {
    "id": "beg_002",
    "title": "Creating and Using Databases/Schemas",
    "level": "beginner",
    "category": "Spark SQL Fundamentals",
    "description": "In Spark SQL, databases and schemas are synonymous terms used to logically group tables. In Unity Catalog, the hierarchy is Catalog > Schema > Table, replacing the legacy two-level Hive metastore model. Proper namespace management is foundational for any enterprise data lake.",
    "code": "-- =============================================\n-- Database/Schema Management in Databricks SQL\n-- Scenario: EU BFSI Medallion Architecture setup\n-- =============================================\n\n-- Create catalog (Unity Catalog only)\nCREATE CATALOG IF NOT EXISTS eu_bfsi_prod\n  COMMENT 'Production catalog for EU Banking, Financial Services & Insurance data';\n\n-- Create schemas for each Medallion layer\nCREATE SCHEMA IF NOT EXISTS eu_bfsi_prod.bronze\n  COMMENT 'Raw ingestion layer — unprocessed data from source systems'\n  MANAGED LOCATION 'abfss://bronze@euadlsprod.dfs.core.windows.net/';\n\nCREATE SCHEMA IF NOT EXISTS eu_bfsi_prod.silver\n  COMMENT 'Cleansed and conformed layer — validated, deduplicated data'\n  MANAGED LOCATION 'abfss://silver@euadlsprod.dfs.core.windows.net/';\n\nCREATE SCHEMA IF NOT EXISTS eu_bfsi_prod.gold\n  COMMENT 'Aggregated business layer — KPIs, reports, ML features'\n  MANAGED LOCATION 'abfss://gold@euadlsprod.dfs.core.windows.net/';\n\n-- Set active catalog and schema for subsequent statements\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- Verify current context\nSELECT current_catalog(), current_database();\n\n-- List all schemas in current catalog\nSHOW SCHEMAS IN eu_bfsi_prod;\n\n-- Drop schema (CASCADE drops all contained objects)\n-- DROP SCHEMA IF EXISTS eu_bfsi_dev.temp CASCADE;",
    "notes": [
      "Unity Catalog uses 3-level namespace: catalog.schema.table — always use fully qualified names in production",
      "MANAGED LOCATION specifies ADLS Gen2 path for all managed tables within the schema",
      "USE CATALOG and USE SCHEMA set the session context — subsequent unqualified table refs resolve to these",
      "Legacy Hive metastore uses hive_metastore as default catalog — migration to Unity Catalog is recommended",
      "Schema-level permissions in Unity Catalog cascade to all tables within — design schemas around access boundaries"
    ],
    "use_case": "Setting up a new EU regulatory reporting workspace in Azure Databricks with Unity Catalog for a retail bank's AML (Anti-Money Laundering) compliance team — each Medallion layer gets its own schema, storage location, and access control boundary."
  },
  {
    "id": "beg_003",
    "title": "SELECT / WHERE / ORDER BY / LIMIT in Spark SQL",
    "level": "beginner",
    "category": "Spark SQL Fundamentals",
    "description": "Foundational DQL (Data Query Language) operations in Spark SQL follow ANSI SQL syntax. Spark SQL supports predicate pushdown to optimize WHERE clause filtering, especially on partitioned Delta tables. ORDER BY in distributed systems is expensive — use SORT WITHIN PARTITIONS for better performance at scale.",
    "code": "-- =============================================\n-- DQL Fundamentals — EU Banking Transactions\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- Basic SELECT with column aliasing\nSELECT\n    txn_id                              AS transaction_id,\n    account_id,\n    UPPER(txn_type)                     AS transaction_type,\n    txn_amount,\n    txn_currency,\n    ROUND(txn_amount * fx_rate, 2)      AS amount_in_eur,\n    txn_timestamp                       AS transaction_ts,\n    DATE(txn_timestamp)                 AS transaction_date\nFROM silver_transactions\nWHERE\n    txn_date >= '2024-01-01'            -- Partition pruning benefit\n    AND txn_date <  '2025-01-01'\n    AND txn_currency IN ('EUR', 'GBP', 'USD')\n    AND txn_amount   BETWEEN 1000 AND 1000000\n    AND txn_status   != 'REVERSED'\n    AND account_id   IS NOT NULL\nORDER BY txn_amount DESC\nLIMIT 500;\n\n-- DISTINCT values\nSELECT DISTINCT txn_currency, txn_type\nFROM silver_transactions\nORDER BY txn_currency;\n\n-- Wildcard LIKE filter (AML keyword scan)\nSELECT txn_id, merchant_name, txn_amount\nFROM silver_transactions\nWHERE merchant_name LIKE '%CRYPTO%'\n   OR merchant_name LIKE '%OFFSHORE%'\nORDER BY txn_amount DESC;\n\n-- NULL-safe comparisons\nSELECT *\nFROM silver_accounts\nWHERE risk_score IS NULL\n   OR credit_limit <=> 0;  -- <=> is NULL-safe equality",
    "notes": [
      "Spark SQL supports ANSI SQL — use standard syntax; Spark-specific extensions are additive",
      "WHERE on partition columns (e.g. txn_date) triggers partition pruning — critical for Delta table performance",
      "ORDER BY is a full sort — it collects all data to a single partition; use only with LIMIT for large datasets",
      "BETWEEN is inclusive on both ends: BETWEEN 1000 AND 1000000 includes both boundary values",
      "The NULL-safe equality operator <=> returns true when both sides are NULL — unlike = which returns NULL"
    ],
    "use_case": "Compliance analysts at a European bank querying the silver transaction layer to identify high-value FX trades for regulatory EMIR reporting — partition pruning on txn_date dramatically reduces query cost on multi-billion-row Delta tables."
  },
  {
    "id": "beg_004",
    "title": "CREATE TABLE USING DELTA",
    "level": "beginner",
    "category": "Delta Lake DDL",
    "description": "Delta Lake is the default table format in Databricks. CREATE TABLE USING DELTA creates a versioned, ACID-compliant table backed by Parquet files and a Delta transaction log. Delta tables support schema enforcement, time travel, and DML operations unlike plain Parquet or CSV tables.",
    "code": "-- =============================================\n-- CREATE Delta Tables — EU BFSI Bronze Layer\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA bronze;\n\n-- Managed Delta table (storage managed by Unity Catalog)\nCREATE TABLE IF NOT EXISTS bronze_transactions (\n    txn_id          STRING          NOT NULL COMMENT 'Unique transaction identifier from source',\n    account_id      STRING          NOT NULL COMMENT 'Source account identifier',\n    counterparty_id STRING                   COMMENT 'Beneficiary account or counterparty',\n    txn_amount      DECIMAL(18, 4)  NOT NULL COMMENT 'Transaction amount in originating currency',\n    txn_currency    STRING(3)       NOT NULL COMMENT 'ISO 4217 currency code',\n    txn_type        STRING                   COMMENT 'CREDIT, DEBIT, FEE, REVERSAL',\n    txn_status      STRING                   COMMENT 'PENDING, COMPLETED, REVERSED, FAILED',\n    txn_timestamp   TIMESTAMP       NOT NULL COMMENT 'Transaction initiation timestamp (UTC)',\n    value_date      DATE                     COMMENT 'Settlement date',\n    source_system   STRING                   COMMENT 'Origin system: CORE_BANKING, SWIFT, SEPA',\n    ingestion_ts    TIMESTAMP       NOT NULL COMMENT 'Databricks ingestion timestamp',\n    batch_id        STRING                   COMMENT 'Pipeline run batch identifier',\n    _rescued_data   STRING                   COMMENT 'Auto Loader rescued data column'\n)\nUSING DELTA\nPARTITIONED BY (txn_date DATE GENERATED ALWAYS AS (DATE(txn_timestamp)))\nCLUSTER BY (account_id, txn_currency)       -- Liquid clustering (post-DBR 13.3)\nCOMMENT 'Raw transaction events ingested from Core Banking and SWIFT — Bronze layer'\nTBLPROPERTIES (\n    'delta.enableChangeDataFeed'            = 'true',\n    'delta.autoOptimize.optimizeWrite'      = 'true',\n    'delta.autoOptimize.autoCompact'        = 'true',\n    'delta.columnMapping.mode'              = 'name',\n    'delta.minReaderVersion'                = '2',\n    'delta.minWriterVersion'                = '5',\n    'pipelines.autoOptimize.zOrderCols'     = 'account_id',\n    'quality.layer'                         = 'bronze',\n    'data.owner'                            = 'data-engineering@bank.eu',\n    'data.classification'                   = 'CONFIDENTIAL'\n);\n\n-- External Delta table (unmanaged — storage controlled externally)\nCREATE TABLE IF NOT EXISTS bronze_swift_messages\nUSING DELTA\nLOCATION 'abfss://bronze@euadlsprod.dfs.core.windows.net/swift/messages/'\nCOMMENT 'SWIFT MT messages — external Delta table';",
    "notes": [
      "USING DELTA is the default in Databricks — explicitly stating it is good practice for documentation clarity",
      "PARTITIONED BY on a GENERATED ALWAYS AS column avoids user error while enforcing partition consistency",
      "CLUSTER BY (Liquid Clustering) is the modern replacement for static partitioning — more flexible re-clustering without full rewrites",
      "TBLPROPERTIES stores metadata and enables features like CDF, column mapping, and auto-optimize",
      "Managed tables: Unity Catalog controls storage. External tables: you control storage but UC manages metadata"
    ],
    "use_case": "A data engineering team at a European custodian bank creating the bronze layer for SWIFT and Core Banking ingestion — Delta's ACID guarantees mean concurrent Auto Loader streams can ingest safely without corrupting the table."
  },
  {
    "id": "beg_005",
    "title": "INSERT INTO and INSERT OVERWRITE",
    "level": "beginner",
    "category": "Delta Lake DML",
    "description": "INSERT INTO appends records to a Delta table atomically. INSERT OVERWRITE replaces table (or partition) data in a single atomic operation, making it ideal for idempotent batch loads. Delta's transaction log ensures both operations are ACID-compliant and recoverable.",
    "code": "-- =============================================\n-- INSERT Operations on Delta Tables\n-- EU BFSI — Loading Reference Data\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA bronze;\n\n-- 1. INSERT INTO — Append rows\nINSERT INTO bronze_transactions\nSELECT\n    txn_id,\n    account_id,\n    counterparty_id,\n    CAST(txn_amount AS DECIMAL(18,4)),\n    txn_currency,\n    txn_type,\n    txn_status,\n    CAST(txn_timestamp AS TIMESTAMP),\n    CAST(value_date AS DATE),\n    source_system,\n    current_timestamp()                 AS ingestion_ts,\n    '${batch_id}'                       AS batch_id,\n    NULL                                AS _rescued_data\nFROM bronze_transactions_staging\nWHERE batch_id = '${batch_id}';\n\n-- 2. INSERT OVERWRITE — Replace partition atomically\nINSERT OVERWRITE eu_bfsi_prod.silver.silver_fx_rates\nPARTITION (rate_date = '2024-12-31')\nSELECT\n    base_currency,\n    target_currency,\n    exchange_rate,\n    source,\n    current_timestamp() AS loaded_ts\nFROM eu_bfsi_prod.bronze.bronze_fx_rates\nWHERE rate_date = '2024-12-31'\n  AND source = 'ECB';\n\n-- 3. INSERT with VALUES (small reference loads)\nINSERT INTO eu_bfsi_prod.silver.dim_currency\n    (currency_code, currency_name, is_eu_currency, created_ts)\nVALUES\n    ('EUR', 'Euro',           true,  current_timestamp()),\n    ('GBP', 'British Pound',  false, current_timestamp()),\n    ('CHF', 'Swiss Franc',    false, current_timestamp()),\n    ('DKK', 'Danish Krone',   false, current_timestamp()),\n    ('SEK', 'Swedish Krona',  false, current_timestamp());\n\n-- 4. INSERT OVERWRITE (full table replace — idempotent)\nINSERT OVERWRITE eu_bfsi_prod.gold.gold_country_risk\nSELECT country_code, risk_tier, updated_date\nFROM eu_bfsi_prod.silver.silver_country_risk_staging;",
    "notes": [
      "INSERT INTO is idempotent only with deduplication logic — without it, rerunning adds duplicates",
      "INSERT OVERWRITE on Delta replaces entire table or specific partition atomically — safe for reruns",
      "Delta's transaction log records every INSERT as a new version — enabling time travel rollback",
      "Use INSERT OVERWRITE for daily/monthly partition refreshes; MERGE INTO for upsert patterns",
      "Schema validation is enforced on INSERT — column types must match or be castable to target schema"
    ],
    "use_case": "Daily ECB FX rate refresh for a European investment bank — INSERT OVERWRITE on the rate_date partition ensures idempotent pipeline runs without risk of duplicate exchange rates corrupting downstream P&L calculations."
  },
  {
    "id": "beg_006",
    "title": "DESCRIBE TABLE / DESCRIBE EXTENDED / SHOW TABLES",
    "level": "beginner",
    "category": "Metadata & Catalog",
    "description": "DESCRIBE and SHOW commands are essential for understanding table schemas, metadata, storage locations, and table properties without opening external tools. In Unity Catalog environments, these commands expose catalog-level metadata including owner, creation time, and data classification tags.",
    "code": "-- =============================================\n-- Metadata Discovery Commands\n-- EU BFSI Data Catalog Exploration\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- 1. Basic column schema\nDESCRIBE TABLE silver_transactions;\n-- Returns: col_name, data_type, comment\n\n-- 2. Extended metadata (storage, properties, stats)\nDESCRIBE TABLE EXTENDED silver_transactions;\n-- Returns: schema + Table Type, Location, Provider,\n--          Owner, Created Time, Table Properties, Statistics\n\n-- 3. Describe specific partition\nDESCRIBE TABLE EXTENDED silver_transactions\nPARTITION (txn_date = '2024-06-01');\n\n-- 4. Show all tables in current schema\nSHOW TABLES IN eu_bfsi_prod.silver;\n\n-- 5. Show tables matching a pattern\nSHOW TABLES IN eu_bfsi_prod.bronze LIKE 'bronze_*';\n\n-- 6. Show all schemas\nSHOW SCHEMAS IN eu_bfsi_prod;\n\n-- 7. Show all catalogs (Unity Catalog)\nSHOW CATALOGS;\n\n-- 8. Show columns\nSHOW COLUMNS IN silver_transactions;\n\n-- 9. Show create statement (DDL reverse engineering)\nSHOW CREATE TABLE eu_bfsi_prod.silver.silver_transactions;\n\n-- 10. Show table properties\nSHOW TBLPROPERTIES eu_bfsi_prod.silver.silver_transactions;\n\n-- 11. Show partitions\nSHOW PARTITIONS eu_bfsi_prod.silver.silver_transactions;\n\n-- 12. Delta-specific: table history\nDESCRIBE HISTORY eu_bfsi_prod.silver.silver_transactions\nLIMIT 20;\n-- Returns: version, timestamp, userId, operation, operationParameters",
    "notes": [
      "DESCRIBE EXTENDED is the go-to for debugging table issues — reveals location, format, owner, and properties",
      "DESCRIBE HISTORY is unique to Delta Lake — shows full audit trail of all DML/DDL operations with timestamps",
      "SHOW CREATE TABLE is invaluable for documentation and reproducing table DDL in new environments",
      "SHOW TBLPROPERTIES helps verify Delta features like CDF, column mapping, and optimize settings",
      "In Unity Catalog, DESCRIBE TABLE EXTENDED shows the securable object's owner and creation metadata"
    ],
    "use_case": "A data governance team at a European insurance company auditing table ownership and storage locations before migrating from Hive Metastore to Unity Catalog — DESCRIBE EXTENDED and SHOW CREATE TABLE help automate the migration DDL generation."
  },
  {
    "id": "beg_007",
    "title": "Basic SQL Functions: CAST, COALESCE, NVL, ISNULL",
    "level": "beginner",
    "category": "Spark SQL Functions",
    "description": "Type conversion and null-handling functions are the building blocks of data quality transformations in the silver layer. CAST enforces type safety, while COALESCE/NVL/ISNULL provide null-handling patterns that prevent downstream aggregation errors in financial reporting.",
    "code": "-- =============================================\n-- Type Conversion & NULL Handling\n-- EU BFSI Silver Layer Data Cleansing\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\nSELECT\n    -- CAST: Explicit type conversion\n    txn_id,\n    CAST(txn_amount AS DECIMAL(18, 4))              AS txn_amount_decimal,\n    CAST(txn_timestamp AS DATE)                     AS txn_date,\n    CAST(txn_timestamp AS STRING)                   AS txn_ts_str,\n    CAST('2024-12-31' AS DATE)                      AS end_date,\n    CAST(is_international AS BOOLEAN)               AS international_flag,\n\n    -- COALESCE: Returns first non-null value\n    COALESCE(merchant_name, counterparty_name,\n             beneficiary_name, 'UNKNOWN')           AS resolved_party_name,\n    COALESCE(fx_rate, 1.0)                          AS fx_rate_with_default,\n    COALESCE(credit_limit, overdraft_limit, 0.0)    AS effective_limit,\n\n    -- NVL: Identical to COALESCE(expr, default) — Hive/Spark compatibility\n    NVL(risk_score, 0)                              AS risk_score_safe,\n    NVL(branch_code, 'ONLINE')                      AS branch_code_safe,\n\n    -- ISNULL / ISNOTNULL / IFNULL\n    ISNULL(reversal_reference)                      AS is_reversal,\n    ISNOTNULL(settlement_date)                      AS is_settled,\n    IFNULL(processing_fee, 0.00)                    AS processing_fee_safe,\n\n    -- NVL2: NVL2(expr, if_not_null, if_null)\n    NVL2(counterparty_country, \n         CONCAT('CROSS-BORDER:', counterparty_country), \n         'DOMESTIC')                                AS txn_geography,\n\n    -- Combining CAST + COALESCE for safe numeric operations\n    CAST(COALESCE(txn_amount, 0) AS DECIMAL(18,4))\n        * CAST(COALESCE(fx_rate, 1.0) AS DECIMAL(10,6))  AS amount_eur,\n\n    -- NULLIF: Returns NULL if two values are equal\n    NULLIF(error_code, '')                          AS error_code_clean,  -- Empty string to NULL\n    NULLIF(txn_count, 0)                            AS txn_count_nonzero  -- 0 to NULL (safe division)\n\nFROM bronze_transactions\nWHERE ingestion_date = current_date();",
    "notes": [
      "COALESCE is ANSI SQL and evaluates arguments left to right — use it over NVL for portability",
      "NULLIF(value, '') is the idiomatic way to convert empty strings to NULL in cleansing pipelines",
      "CAST is strict — use TRY_CAST to return NULL instead of throwing on invalid conversions",
      "DECIMAL(18,4) is the recommended type for financial amounts — avoids floating-point precision errors",
      "NVL is Hive-compatible syntax supported in Spark SQL; prefer COALESCE in new code for ANSI compliance"
    ],
    "use_case": "Silver layer data cleansing pipeline at a European payments processor — raw Core Banking data arrives with mixed types and nulls; CAST and COALESCE transforms ensure downstream gold layer aggregations produce accurate SEPA reporting figures."
  },
  {
    "id": "beg_008",
    "title": "Reading Files Directly with SQL (CSV, JSON, Parquet)",
    "level": "beginner",
    "category": "Spark SQL Fundamentals",
    "description": "Spark SQL supports querying files directly without defining a table, using the `format.\\`path\\`` syntax. This is useful for data exploration, one-time loads, and Auto Loader alternative ingestion. Delta, Parquet, JSON, CSV, and Avro are all supported inline in SELECT statements.",
    "code": "-- =============================================\n-- Direct File Querying in Spark SQL\n-- EU BFSI — Exploring Raw Ingestion Files\n-- =============================================\n\n-- 1. Read CSV directly (with inferred schema)\nSELECT *\nFROM csv.`abfss://raw@euadlsprod.dfs.core.windows.net/ecb_fx_rates/2024/12/31/`\nLIMIT 100;\n\n-- 2. Read CSV with explicit schema and options\nSELECT *\nFROM read_files(\n    'abfss://raw@euadlsprod.dfs.core.windows.net/ecb_fx_rates/2024/',\n    format => 'csv',\n    header => 'true',\n    inferSchema => 'true',\n    sep => ','\n);\n\n-- 3. Read Parquet file/directory\nSELECT\n    account_id,\n    balance,\n    as_of_date\nFROM parquet.`abfss://bronze@euadlsprod.dfs.core.windows.net/account_balances/year=2024/month=12/`\nWHERE as_of_date >= '2024-12-01';\n\n-- 4. Read JSON (SWIFT MT103 messages)\nSELECT\n    get_json_object(value, '$.MessageType')     AS msg_type,\n    get_json_object(value, '$.TransactionRef')  AS txn_ref,\n    get_json_object(value, '$.Amount.Value')    AS amount,\n    get_json_object(value, '$.Amount.Currency') AS currency,\n    get_json_object(value, '$.ValueDate')       AS value_date\nFROM json.`abfss://raw@euadlsprod.dfs.core.windows.net/swift/mt103/2024/12/`\nLIMIT 200;\n\n-- 5. Read Delta files directly\nSELECT * FROM delta.`abfss://bronze@euadlsprod.dfs.core.windows.net/transactions/`\nLIMIT 50;\n\n-- 6. Create a table from file (CTAS)\nCREATE TABLE IF NOT EXISTS eu_bfsi_prod.bronze.bronze_ecb_fx\nUSING DELTA\nAS\nSELECT\n    _metadata.file_path          AS source_file,\n    _metadata.file_modification_time AS file_ts,\n    *\nFROM read_files(\n    'abfss://raw@euadlsprod.dfs.core.windows.net/ecb_fx_rates/',\n    format => 'csv',\n    header => 'true',\n    inferSchema => 'true'\n);",
    "notes": [
      "format.`path` syntax works for delta, parquet, json, csv, avro, orc — convenient for exploration",
      "read_files() is the modern Databricks SQL function with richer options including schema hints",
      "_metadata columns (file_path, file_size, file_modification_time) are auto-populated by Spark for traceability",
      "Direct file queries don't benefit from Delta optimizations — create a Delta table for production use",
      "CTAS (CREATE TABLE AS SELECT) is the fastest way to materialize exploratory queries into Delta tables"
    ],
    "use_case": "A data engineer at a European asset manager exploring newly landed ECB exchange rate CSV files in ADLS Gen2 before defining the formal bronze table schema — direct SQL querying saves time compared to writing full PySpark read/display code."
  },
  {
    "id": "int_001",
    "title": "CTEs (Common Table Expressions) in Spark SQL",
    "level": "intermediate",
    "category": "Query Structuring",
    "description": "CTEs (WITH clauses) improve query readability and maintainability by naming intermediate result sets. Spark SQL supports multiple CTEs in a single WITH block and recursive CTEs. In BFSI pipelines, CTEs help decompose complex multi-step transformations into readable, testable units.",
    "code": "-- =============================================\n-- CTEs for Complex BFSI Analytics\n-- AML Transaction Monitoring Pipeline\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA gold;\n\nWITH\n-- CTE 1: Base transaction data with FX normalization\nnormalized_txns AS (\n    SELECT\n        t.txn_id,\n        t.account_id,\n        t.txn_amount,\n        t.txn_currency,\n        t.txn_amount * COALESCE(fx.exchange_rate, 1.0)  AS amount_eur,\n        t.txn_type,\n        t.txn_timestamp,\n        DATE(t.txn_timestamp)                            AS txn_date,\n        t.counterparty_country,\n        t.source_system\n    FROM eu_bfsi_prod.silver.silver_transactions t\n    LEFT JOIN eu_bfsi_prod.silver.silver_fx_rates fx\n        ON t.txn_currency = fx.from_currency\n        AND DATE(t.txn_timestamp) = fx.rate_date\n        AND fx.to_currency = 'EUR'\n    WHERE t.txn_date BETWEEN '2024-01-01' AND '2024-12-31'\n      AND t.txn_status = 'COMPLETED'\n),\n\n-- CTE 2: Daily account aggregates\ndaily_account_aggregates AS (\n    SELECT\n        account_id,\n        txn_date,\n        COUNT(*)                    AS txn_count,\n        SUM(amount_eur)             AS daily_debit_eur,\n        MAX(amount_eur)             AS max_single_txn_eur,\n        COUNT(DISTINCT counterparty_country) AS unique_countries\n    FROM normalized_txns\n    WHERE txn_type = 'DEBIT'\n    GROUP BY account_id, txn_date\n),\n\n-- CTE 3: Accounts with high-risk patterns\nhigh_risk_accounts AS (\n    SELECT\n        account_id,\n        AVG(daily_debit_eur)        AS avg_daily_debit,\n        MAX(daily_debit_eur)        AS peak_daily_debit,\n        SUM(txn_count)              AS total_txns,\n        MAX(unique_countries)       AS max_countries_in_day\n    FROM daily_account_aggregates\n    GROUP BY account_id\n    HAVING  AVG(daily_debit_eur) > 50000\n        OR  MAX(unique_countries) >= 5\n        OR  MAX(daily_debit_eur)  > 250000\n),\n\n-- CTE 4: Account profile enrichment\naccount_profile AS (\n    SELECT\n        a.account_id,\n        a.customer_id,\n        a.account_type,\n        a.risk_tier,\n        c.customer_name,\n        c.nationality,\n        c.pep_flag,         -- Politically Exposed Person\n        c.sanction_flag\n    FROM eu_bfsi_prod.silver.silver_accounts a\n    JOIN eu_bfsi_prod.silver.silver_customers c\n        ON a.customer_id = c.customer_id\n)\n\n-- Final output: AML alert candidates\nSELECT\n    hr.account_id,\n    ap.customer_name,\n    ap.nationality,\n    ap.pep_flag,\n    ap.sanction_flag,\n    ap.risk_tier,\n    hr.avg_daily_debit,\n    hr.peak_daily_debit,\n    hr.total_txns,\n    hr.max_countries_in_day,\n    CASE\n        WHEN ap.sanction_flag = true         THEN 'CRITICAL'\n        WHEN ap.pep_flag = true              THEN 'HIGH'\n        WHEN hr.peak_daily_debit > 500000   THEN 'HIGH'\n        WHEN hr.max_countries_in_day >= 10  THEN 'MEDIUM'\n        ELSE                                     'LOW'\n    END                                     AS alert_priority,\n    current_timestamp()                     AS alert_generated_ts\nFROM high_risk_accounts hr\nJOIN account_profile ap ON hr.account_id = ap.account_id\nORDER BY alert_priority, hr.peak_daily_debit DESC;",
    "notes": [
      "CTEs are not materialized by default in Spark — they are inlined into the query plan; use temp views for reuse across multiple queries",
      "Multiple CTEs in one WITH block are separated by commas — later CTEs can reference earlier ones",
      "CTEs vastly improve query readability vs deeply nested subqueries — prefer them in code reviews",
      "For expensive intermediate results reused multiple times, consider CACHE TABLE or a temp view materialization",
      "Spark SQL supports recursive CTEs (WITH RECURSIVE) since Spark 3.4 — useful for hierarchy traversal"
    ],
    "use_case": "AML transaction monitoring pipeline at a pan-European retail bank — CTEs decompose the complex multi-join, multi-aggregation alert generation query into readable stages that can be individually unit-tested and reviewed by compliance teams."
  },
  {
    "id": "int_002",
    "title": "Window Functions in Spark SQL",
    "level": "intermediate",
    "category": "Analytical Functions",
    "description": "Window functions perform calculations across a set of rows related to the current row without collapsing the result set. Essential for running totals, rankings, lag/lead comparisons, and period-over-period analytics in financial reporting. Spark SQL implements the full ANSI SQL window function standard.",
    "code": "-- =============================================\n-- Window Functions — EU Banking Analytics\n-- Account Balance & Transaction Trend Analysis\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA gold;\n\nSELECT\n    account_id,\n    txn_date,\n    txn_id,\n    txn_amount,\n    txn_type,\n\n    -- RANKING functions\n    ROW_NUMBER() OVER (\n        PARTITION BY account_id\n        ORDER BY txn_timestamp DESC\n    )                                               AS txn_recency_rank,\n\n    RANK() OVER (\n        PARTITION BY account_id, MONTH(txn_date)\n        ORDER BY txn_amount DESC\n    )                                               AS monthly_amount_rank,\n\n    DENSE_RANK() OVER (\n        PARTITION BY account_id\n        ORDER BY DATE(txn_date)\n    )                                               AS day_sequence,\n\n    -- VALUE functions: LAG / LEAD\n    LAG(txn_amount, 1, 0) OVER (\n        PARTITION BY account_id\n        ORDER BY txn_timestamp\n    )                                               AS prev_txn_amount,\n\n    txn_amount - LAG(txn_amount, 1, 0) OVER (\n        PARTITION BY account_id\n        ORDER BY txn_timestamp\n    )                                               AS amount_delta_from_prev,\n\n    LEAD(txn_date, 1) OVER (\n        PARTITION BY account_id\n        ORDER BY txn_timestamp\n    )                                               AS next_txn_date,\n\n    -- AGGREGATE window functions\n    SUM(txn_amount) OVER (\n        PARTITION BY account_id\n        ORDER BY txn_timestamp\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    )                                               AS running_total,\n\n    AVG(txn_amount) OVER (\n        PARTITION BY account_id\n        ORDER BY txn_date\n        ROWS BETWEEN 6 PRECEDING AND CURRENT ROW\n    )                                               AS rolling_7day_avg,\n\n    SUM(txn_amount) OVER (\n        PARTITION BY account_id, YEAR(txn_date), MONTH(txn_date)\n    )                                               AS monthly_total,\n\n    -- FIRST_VALUE / LAST_VALUE\n    FIRST_VALUE(txn_amount) OVER (\n        PARTITION BY account_id, DATE(txn_date)\n        ORDER BY txn_timestamp\n        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    )                                               AS first_txn_of_day,\n\n    LAST_VALUE(txn_amount) OVER (\n        PARTITION BY account_id, DATE(txn_date)\n        ORDER BY txn_timestamp\n        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    )                                               AS last_txn_of_day,\n\n    -- NTILE: Quartile bucketing\n    NTILE(4) OVER (\n        PARTITION BY YEAR(txn_date)\n        ORDER BY txn_amount\n    )                                               AS amount_quartile,\n\n    -- PERCENT_RANK / CUME_DIST\n    ROUND(PERCENT_RANK() OVER (\n        PARTITION BY account_id\n        ORDER BY txn_amount\n    ), 4)                                           AS pct_rank_in_account,\n\n    ROUND(CUME_DIST() OVER (\n        ORDER BY txn_amount\n    ), 4)                                           AS cumulative_distribution\n\nFROM eu_bfsi_prod.silver.silver_transactions\nWHERE txn_date BETWEEN '2024-01-01' AND '2024-12-31'\n  AND txn_status = 'COMPLETED'\n  AND txn_type = 'DEBIT'\nQUALIFY txn_recency_rank <= 10;  -- Databricks-specific: filter on window result",
    "notes": [
      "PARTITION BY is optional — omitting it makes the window span the entire dataset (use carefully on large tables)",
      "ROWS BETWEEN vs RANGE BETWEEN: ROWS is physical (N rows before/after), RANGE is logical (based on ORDER BY value)",
      "QUALIFY clause (Databricks extension) filters on window function results without a subquery wrapper",
      "LAG/LEAD default value (3rd parameter) prevents NULL in first/last rows — always provide defaults for financial calcs",
      "Window functions are computed after WHERE/GROUP BY/HAVING but before ORDER BY and LIMIT"
    ],
    "use_case": "A European investment bank's risk analytics team calculating rolling 7-day average transaction amounts and running totals per account to detect structuring patterns (breaking large transactions into smaller ones to evade AML thresholds) — window functions make this a single-pass SQL query."
  },
  {
    "id": "int_003",
    "title": "Complex Types: Arrays, Maps, Structs — EXPLODE, COLLECT_LIST",
    "level": "intermediate",
    "category": "Complex Data Types",
    "description": "Spark SQL's support for nested data types (ARRAY, MAP, STRUCT) enables working with semi-structured data natively without denormalization. EXPLODE converts arrays/maps into rows, while COLLECT_LIST/COLLECT_SET aggregate rows back into arrays. Critical for processing JSON-based financial messages.",
    "code": "-- =============================================\n-- Complex Types in Spark SQL\n-- EU BFSI — SWIFT & Trade Finance Messages\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- 1. Querying STRUCT fields (dot notation)\nSELECT\n    txn_id,\n    counterparty.bank_name                  AS cp_bank,\n    counterparty.bic_code                   AS cp_bic,\n    counterparty.country                    AS cp_country,\n    amount_details.value                    AS txn_value,\n    amount_details.currency                 AS txn_currency,\n    amount_details.fx_rate                  AS applied_rate\nFROM silver_swift_mt103\nWHERE counterparty.country NOT IN ('GB', 'US', 'EU')\n  AND amount_details.value > 100000;\n\n-- 2. EXPLODE array — one row per array element\nSELECT\n    trade_id,\n    leg_idx,               -- Position in original array\n    leg.leg_type           AS leg_type,\n    leg.notional           AS leg_notional,\n    leg.currency           AS leg_currency,\n    leg.maturity_date      AS leg_maturity\nFROM silver_trade_legs\nLATERAL VIEW POSEXPLODE(trade_legs) AS leg_idx, leg\nWHERE leg.leg_type IN ('FIXED', 'FLOATING');\n\n-- 3. EXPLODE map — one row per key-value pair\nSELECT\n    account_id,\n    attr_key,\n    attr_value\nFROM silver_account_attributes\nLATERAL VIEW EXPLODE(attribute_map) AS attr_key, attr_value\nWHERE attr_key IN ('RISK_TIER', 'KYC_STATUS', 'PRODUCT_CODE');\n\n-- 4. COLLECT_LIST — aggregate rows into array\nSELECT\n    account_id,\n    COLLECT_LIST(txn_id)                    AS all_txn_ids,\n    COLLECT_LIST(\n        STRUCT(txn_id, txn_amount, txn_date)\n    )                                       AS txn_details,\n    SIZE(COLLECT_LIST(txn_id))              AS txn_count,\n    COLLECT_SET(txn_currency)               AS unique_currencies,\n    ARRAY_SORT(COLLECT_SET(counterparty_country)) AS countries_dealt_with\nFROM silver_transactions\nWHERE txn_date >= '2024-01-01'\nGROUP BY account_id;\n\n-- 5. Array manipulation functions\nSELECT\n    account_id,\n    flagged_txn_ids,\n    ARRAY_CONTAINS(flagged_txn_ids, '${alert_txn_id}')   AS in_alert_list,\n    ARRAY_SIZE(flagged_txn_ids)                           AS flag_count,\n    FILTER(flagged_txn_ids, x -> x LIKE 'TXN-EU%')       AS eu_flagged_only,\n    TRANSFORM(txn_amounts, x -> x * 1.20)                AS amounts_with_vat,\n    AGGREGATE(txn_amounts, 0D, (acc, x) -> acc + x)      AS total_amount,\n    FLATTEN(ARRAY(txn_ids_q1, txn_ids_q2, txn_ids_q3, txn_ids_q4)) AS all_year_txns\nFROM silver_account_summary;\n\n-- 6. Struct construction and from_json\nSELECT\n    txn_id,\n    STRUCT(\n        account_id      AS account,\n        txn_amount      AS amount,\n        txn_currency    AS currency\n    )                                       AS txn_core_struct,\n    FROM_JSON(\n        raw_payload,\n        'STRUCT<sender_bic:STRING, receiver_bic:STRING, ref:STRING>'\n    )                                       AS parsed_payload\nFROM bronze_transactions;",
    "notes": [
      "POSEXPLODE returns both the index and the element — use it when array position matters (e.g., trade legs order)",
      "COLLECT_LIST preserves duplicates; COLLECT_SET removes them — choose based on whether deduplication is needed",
      "FILTER, TRANSFORM, AGGREGATE are higher-order functions that accept lambda expressions — powerful for array operations",
      "from_json/to_json enable parsing JSON strings into typed structs without exploding to rows",
      "Struct columns support dot notation for field access — nested structs can be chained: col.nested.field"
    ],
    "use_case": "Processing SWIFT MT103 and ISO 20022 trade finance messages at a European correspondent bank — JSON payloads with nested arrays of trade legs and MAP-typed attribute collections are parsed and exploded in the silver layer transformation pipeline."
  },
  {
    "id": "int_004",
    "title": "JOIN Types in Spark SQL",
    "level": "intermediate",
    "category": "Query Structuring",
    "description": "Spark SQL supports all standard SQL JOIN types plus Spark-specific extensions. Understanding join strategies (broadcast, shuffle hash, sort merge) is critical for performance in large-scale BFSI joins between fact and dimension tables. The USING clause simplifies equi-joins on identically named columns.",
    "code": "-- =============================================\n-- JOIN Patterns in EU BFSI Analytics\n-- Transaction Enrichment Pipeline\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA gold;\n\n-- 1. INNER JOIN — most common, only matching rows\nSELECT\n    t.txn_id,\n    t.account_id,\n    t.txn_amount,\n    a.account_type,\n    a.risk_tier,\n    c.customer_name\nFROM silver_transactions t\nINNER JOIN silver_accounts a ON t.account_id = a.account_id\nINNER JOIN silver_customers c ON a.customer_id = c.customer_id\nWHERE t.txn_date = current_date();\n\n-- 2. LEFT JOIN — keep all transactions, enrich where possible\nSELECT\n    t.txn_id,\n    t.txn_amount,\n    t.txn_currency,\n    COALESCE(fx.exchange_rate, 1.0)     AS fx_rate,\n    t.txn_amount * COALESCE(fx.exchange_rate, 1.0) AS amount_eur\nFROM silver_transactions t\nLEFT JOIN silver_fx_rates fx\n    ON t.txn_currency = fx.from_currency\n    AND DATE(t.txn_timestamp) = fx.rate_date\n    AND fx.to_currency = 'EUR';\n\n-- 3. ANTI JOIN — find unmatched records (data quality)\nSELECT t.txn_id, t.account_id, t.txn_amount\nFROM bronze_transactions t\nLEFT ANTI JOIN silver_transactions s ON t.txn_id = s.txn_id\nWHERE t.ingestion_date = current_date();\n-- Finds bronze records not yet promoted to silver\n\n-- 4. SEMI JOIN — filter on existence (no columns from right side)\nSELECT account_id, customer_id, risk_tier\nFROM silver_accounts\nWHERE account_id IN (\n    SELECT DISTINCT account_id\n    FROM silver_transactions\n    WHERE txn_amount > 1000000\n      AND txn_date >= '2024-01-01'\n);\n-- Equivalent SEMI JOIN syntax:\nSELECT a.account_id, a.customer_id, a.risk_tier\nFROM silver_accounts a\nLEFT SEMI JOIN silver_transactions t\n    ON a.account_id = t.account_id\n    AND t.txn_amount > 1000000\n    AND t.txn_date >= '2024-01-01';\n\n-- 5. CROSS JOIN — use with extreme caution (Cartesian product)\nSELECT\n    c.currency_code,\n    r.region_code,\n    CONCAT(c.currency_code, '_', r.region_code) AS segment_key\nFROM dim_currency c\nCROSS JOIN dim_region r;  -- Safe only on small dimension tables\n\n-- 6. USING clause — same-named column equi-join\nSELECT t.txn_id, t.txn_amount, a.account_type\nFROM silver_transactions t\nJOIN silver_accounts a USING (account_id);\n\n-- 7. NON-EQUI JOIN — join with range condition\nSELECT\n    t.txn_id,\n    t.txn_amount,\n    r.risk_category\nFROM silver_transactions t\nJOIN dim_amount_risk_bands r\n    ON t.txn_amount BETWEEN r.min_amount AND r.max_amount\n    AND t.txn_currency = r.currency;\n\n-- 8. FULL OUTER JOIN — reconciliation pattern\nSELECT\n    COALESCE(s.txn_id, t.txn_id)           AS txn_id,\n    s.txn_amount                            AS source_amount,\n    t.txn_amount                            AS target_amount,\n    s.txn_amount - t.txn_amount             AS variance,\n    CASE\n        WHEN s.txn_id IS NULL THEN 'TARGET_ONLY'\n        WHEN t.txn_id IS NULL THEN 'SOURCE_ONLY'\n        ELSE 'MATCHED'\n    END                                     AS reconciliation_status\nFROM silver_source_transactions s\nFULL OUTER JOIN silver_target_transactions t ON s.txn_id = t.txn_id;",
    "notes": [
      "ANTI JOIN and SEMI JOIN are Spark SQL extensions — they're highly efficient because the right table is only used for filtering",
      "Broadcast join is automatically chosen when one side is small (<= spark.sql.autoBroadcastJoinThreshold, default 10MB)",
      "FULL OUTER JOIN is the reconciliation pattern — ideal for source-to-target data quality checks",
      "Non-equi joins (BETWEEN, >, <) cannot use hash join — Spark falls back to sort-merge join, which can be expensive",
      "The USING clause automatically deduplicates the join column in the output — the ON clause keeps both sides"
    ],
    "use_case": "Transaction enrichment pipeline in a European bank's silver layer — LEFT JOINs add FX rates and account details while ANTI JOINs identify bronze records that failed silver promotion, enabling automated re-processing alerts in the monitoring dashboard."
  },
  {
    "id": "int_005",
    "title": "GROUP BY Extensions: ROLLUP, CUBE, GROUPING SETS",
    "level": "intermediate",
    "category": "Analytical Functions",
    "description": "ROLLUP, CUBE, and GROUPING SETS are SQL extensions that generate multiple levels of aggregation in a single query pass. Essential for financial reporting where subtotals, grand totals, and cross-dimensional summaries are needed simultaneously — replacing multiple UNION ALL queries with a single, efficient operation.",
    "code": "-- =============================================\n-- GROUP BY Extensions — EU BFSI Regulatory Reporting\n-- EBA COREP / FINREP Multi-dimensional Aggregation\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA gold;\n\n-- 1. ROLLUP — hierarchical subtotals (right-to-left)\n-- Produces: (region, country, currency), (region, country), (region), ()\nSELECT\n    COALESCE(region, 'ALL_REGIONS')         AS region,\n    COALESCE(country_code, 'ALL_COUNTRIES') AS country_code,\n    COALESCE(txn_currency, 'ALL_CURRENCIES') AS txn_currency,\n    SUM(txn_amount_eur)                     AS total_amount_eur,\n    COUNT(DISTINCT txn_id)                  AS txn_count,\n    COUNT(DISTINCT account_id)              AS unique_accounts,\n    GROUPING(region)                        AS is_region_subtotal,\n    GROUPING(country_code)                  AS is_country_subtotal,\n    GROUPING(txn_currency)                  AS is_currency_subtotal,\n    GROUPING_ID(region, country_code, txn_currency) AS grouping_level\nFROM gold_txn_summary\nWHERE report_period = '2024-Q4'\nGROUP BY ROLLUP(region, country_code, txn_currency)\nORDER BY grouping_level DESC, region, country_code, txn_currency;\n\n-- 2. CUBE — all combinations of GROUP BY columns\n-- Produces every combination: 2^N grouping sets for N columns\nSELECT\n    COALESCE(txn_type, 'ALL')               AS txn_type,\n    COALESCE(risk_tier, 'ALL')              AS risk_tier,\n    COALESCE(product_code, 'ALL')           AS product_code,\n    SUM(txn_amount_eur)                     AS total_eur,\n    AVG(txn_amount_eur)                     AS avg_eur,\n    COUNT(*)                                AS txn_count\nFROM gold_txn_summary\nGROUP BY CUBE(txn_type, risk_tier, product_code)\nHAVING SUM(txn_amount_eur) > 1000000\nORDER BY GROUPING_ID(txn_type, risk_tier, product_code) DESC;\n\n-- 3. GROUPING SETS — explicit control over which combinations\n-- For FINREP: need (country), (product), (country, product), and grand total\nSELECT\n    country_code,\n    product_code,\n    txn_currency,\n    SUM(exposure_amount_eur)                AS total_exposure,\n    COUNT(DISTINCT counterparty_id)         AS counterparty_count,\n    GROUPING_ID(country_code, product_code, txn_currency) AS grp_id\nFROM gold_regulatory_exposure\nWHERE reporting_date = '2024-12-31'\nGROUP BY GROUPING SETS (\n    (country_code),\n    (product_code),\n    (txn_currency),\n    (country_code, product_code),\n    (country_code, txn_currency),\n    ()  -- Grand total row\n)\nORDER BY grp_id DESC, total_exposure DESC;",
    "notes": [
      "GROUPING() returns 1 if column is aggregated (subtotal row), 0 if it's a detail row — use with COALESCE for labeling",
      "GROUPING_ID() returns a bit-mask integer identifying which columns are aggregated — useful for downstream filtering",
      "ROLLUP(a, b, c) is equivalent to GROUPING SETS ((a,b,c),(a,b),(a),()) — N+1 sets for N columns",
      "CUBE(a,b,c) generates 2^N grouping sets — use GROUPING SETS for explicit control to avoid unnecessary computation",
      "These extensions are critical for single-pass generation of regulatory reports (COREP, FINREP, EMIR) at multiple hierarchy levels"
    ],
    "use_case": "Generating EBA FINREP regulatory exposure reports for a pan-European bank — GROUPING SETS produces country-level, product-level, and grand-total summaries in a single optimized query, replacing the 5 separate UNION ALL queries that previously caused the daily reporting job to exceed the SLA window."
  },
  {
    "id": "int_006",
    "title": "PIVOT and UNPIVOT in Spark SQL",
    "level": "intermediate",
    "category": "Analytical Functions",
    "description": "PIVOT rotates rows into columns, transforming long-format data into wide-format reports. UNPIVOT is the reverse operation. These are essential for financial matrix reports, period-over-period comparisons, and cross-tabulation reporting required by regulatory bodies.",
    "code": "-- =============================================\n-- PIVOT / UNPIVOT — EU BFSI Reporting\n-- Monthly P&L and Exposure Matrix Reports\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA gold;\n\n-- 1. PIVOT: Monthly transaction volumes by product type\nSELECT *\nFROM (\n    SELECT\n        account_id,\n        DATE_FORMAT(txn_date, 'MMM-yyyy')   AS txn_month,\n        product_code,\n        txn_amount_eur\n    FROM gold_txn_summary\n    WHERE txn_date BETWEEN '2024-01-01' AND '2024-12-31'\n)\nPIVOT (\n    ROUND(SUM(txn_amount_eur) / 1000000, 2) AS vol_meur  -- Millions EUR\n    FOR product_code IN (\n        'MORTGAGE'  AS mortgage,\n        'PERSONAL'  AS personal_loan,\n        'CURRENT'   AS current_acct,\n        'SAVINGS'   AS savings_acct,\n        'TRADE_FIN' AS trade_finance,\n        'FX_SPOT'   AS fx_spot\n    )\n);\n-- Output columns: account_id, txn_month, mortgage, personal_loan, ...\n\n-- 2. PIVOT with multiple aggregations per column\nSELECT *\nFROM (\n    SELECT country_code, risk_tier, txn_amount_eur\n    FROM gold_regulatory_exposure\n    WHERE reporting_date = '2024-12-31'\n)\nPIVOT (\n    ROUND(SUM(txn_amount_eur)/1e9, 2)   AS sum_geur,\n    COUNT(*)                            AS cnt\n    FOR risk_tier IN ('LOW' AS low, 'MEDIUM' AS med, 'HIGH' AS high, 'CRITICAL' AS crit)\n);\n-- Output: country_code, low_sum_geur, low_cnt, med_sum_geur, med_cnt, ...\n\n-- 3. Dynamic PIVOT (using Spark SQL + Python wrapper)\n-- Note: Spark SQL PIVOT requires static column list\n-- For dynamic pivoting, generate SQL in PySpark:\n/*\npivot_cols = spark.sql(\"\"\"\n    SELECT DISTINCT risk_tier FROM gold_regulatory_exposure\n    WHERE reporting_date = '2024-12-31'\n    ORDER BY risk_tier\n\"\"\").rdd.flatMap(lambda x: x).collect()\n\npivot_expr = ', '.join([f\"'{col}' AS {col.lower().replace(' ','_')}\" for col in pivot_cols])\ndynamic_pivot_sql = f\"\"\"\n    SELECT * FROM (\n        SELECT country_code, risk_tier, txn_amount_eur\n        FROM gold_regulatory_exposure WHERE reporting_date = '2024-12-31'\n    ) PIVOT (SUM(txn_amount_eur) FOR risk_tier IN ({pivot_expr}))\n\"\"\"\nresult = spark.sql(dynamic_pivot_sql)\n*/\n\n-- 4. UNPIVOT: Wide → Long format\nSELECT account_id, metric_name, metric_value\nFROM (\n    SELECT\n        account_id,\n        balance_current,\n        balance_savings,\n        balance_loan,\n        credit_limit\n    FROM gold_account_balances\n    WHERE snapshot_date = '2024-12-31'\n)\nUNPIVOT (\n    metric_value FOR metric_name IN (\n        balance_current  AS 'CURRENT_BALANCE',\n        balance_savings  AS 'SAVINGS_BALANCE',\n        balance_loan     AS 'LOAN_BALANCE',\n        credit_limit     AS 'CREDIT_LIMIT'\n    )\n)\nWHERE metric_value IS NOT NULL;",
    "notes": [
      "PIVOT requires specifying column values statically in SQL — use PySpark to generate dynamic pivot SQL for unknown categories",
      "PIVOT with multiple aggregate functions generates N*M columns (N pivot values × M aggregations) — keep combinations manageable",
      "UNPIVOT is essentially a structured STACK operation — equivalent to multiple UNION ALLs but more readable",
      "Pivoted DataFrames are often used to feed Power BI or Excel-based reports via JDBC/ODBC Databricks SQL endpoints",
      "Consider materialized gold views for pivoted reporting tables — avoids re-running expensive pivots on every report query"
    ],
    "use_case": "Building an EBA COREP capital adequacy matrix report for a European universal bank — PIVOT transforms long-format risk exposure data (one row per country-tier) into the wide-format matrix table (countries as rows, risk tiers as columns) required by the regulatory submission template."
  },
  {
    "id": "int_007",
    "title": "Date and Timestamp Functions",
    "level": "intermediate",
    "category": "Spark SQL Functions",
    "description": "Date and timestamp handling is critical in BFSI for interest calculations, period-end reporting, settlement date computations, and timezone normalization. Spark SQL provides a rich set of date functions fully compatible with ANSI SQL, plus Databricks-specific extensions for timezone-aware operations.",
    "code": "-- =============================================\n-- Date & Timestamp Functions\n-- EU BFSI — Reporting Period & Settlement Calcs\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\nSELECT\n    txn_id,\n    txn_timestamp,\n\n    -- ── Current values ──────────────────────\n    current_date()                          AS today,\n    current_timestamp()                     AS now_utc,\n    now()                                   AS now_alias,\n\n    -- ── Extraction ──────────────────────────\n    YEAR(txn_timestamp)                     AS txn_year,\n    MONTH(txn_timestamp)                    AS txn_month,\n    DAY(txn_timestamp)                      AS txn_day,\n    DAYOFWEEK(txn_timestamp)                AS day_of_week,  -- 1=Sun\n    DAYOFYEAR(txn_timestamp)                AS day_of_year,\n    WEEKOFYEAR(txn_timestamp)               AS week_number,\n    QUARTER(txn_timestamp)                  AS txn_quarter,\n    HOUR(txn_timestamp)                     AS txn_hour,\n    MINUTE(txn_timestamp)                   AS txn_minute,\n\n    -- ── Formatting ──────────────────────────\n    DATE_FORMAT(txn_timestamp, 'yyyy-MM-dd')            AS date_str,\n    DATE_FORMAT(txn_timestamp, 'dd/MM/yyyy HH:mm:ss')   AS eu_datetime_str,\n    DATE_FORMAT(txn_timestamp, 'MMM yyyy')              AS month_label,\n    TO_DATE(txn_timestamp)                              AS date_only,\n\n    -- ── Arithmetic ──────────────────────────\n    DATE_ADD(txn_timestamp, 2)              AS value_date_t2,   -- T+2 settlement\n    DATE_ADD(txn_timestamp, 30)             AS payment_due,\n    DATE_SUB(txn_timestamp, 1)              AS prev_business_approx,\n    ADD_MONTHS(txn_timestamp, 3)            AS quarterly_review,\n    ADD_MONTHS(LAST_DAY(txn_timestamp), 1)  AS next_month_end,\n    LAST_DAY(txn_timestamp)                 AS month_end,\n    TRUNC(txn_timestamp, 'MM')              AS month_start,\n    TRUNC(txn_timestamp, 'YEAR')            AS year_start,\n    TRUNC(txn_timestamp, 'DD')              AS start_of_day,\n\n    -- ── Differences ─────────────────────────\n    DATEDIFF(current_date(), DATE(txn_timestamp))       AS days_since_txn,\n    MONTHS_BETWEEN(current_date(), DATE(txn_timestamp)) AS months_since_txn,\n\n    -- ── Timezone Conversion ──────────────────\n    CONVERT_TIMEZONE('UTC', 'Europe/London', txn_timestamp)  AS ts_london,\n    CONVERT_TIMEZONE('UTC', 'Europe/Paris',  txn_timestamp)  AS ts_paris,\n    FROM_UTC_TIMESTAMP(txn_timestamp, 'Europe/Berlin')       AS ts_berlin,\n    TO_UTC_TIMESTAMP(txn_timestamp, 'Europe/Warsaw')         AS back_to_utc,\n\n    -- ── Unix timestamps ──────────────────────\n    UNIX_TIMESTAMP(txn_timestamp)           AS epoch_seconds,\n    FROM_UNIXTIME(epoch_timestamp)          AS from_epoch,\n    TIMESTAMP_MILLIS(epoch_millis)          AS from_millis,\n\n    -- ── Parsing ─────────────────────────────\n    TO_TIMESTAMP('31/12/2024 23:59:59', 'dd/MM/yyyy HH:mm:ss') AS parsed_ts,\n    TO_DATE('31-12-2024', 'dd-MM-yyyy')                         AS parsed_date,\n    TRY_TO_TIMESTAMP('not-a-date', 'yyyy-MM-dd')                AS safe_parse  -- Returns NULL\n\nFROM silver_transactions\nWHERE txn_date = current_date() - INTERVAL 1 DAY;  -- Yesterday's transactions",
    "notes": [
      "All Databricks timestamps are stored in UTC — always use CONVERT_TIMEZONE for European reporting (CET/CEST shift)",
      "TRUNC(ts, 'MM') gives first day of month — essential for monthly report period boundaries",
      "DATE_ADD operates on calendar days; business day calculations require a custom calendar table join",
      "TRY_TO_TIMESTAMP / TRY_CAST return NULL on parse failure instead of throwing — always prefer in ETL",
      "MONTHS_BETWEEN returns a decimal — ROUND to integer for whole-month differences in interest calculations"
    ],
    "use_case": "SEPA Direct Debit settlement date calculation engine at a European payment institution — T+2 settlement dates, CET business day reporting windows, and end-of-month position snapshots all require precise date arithmetic with timezone awareness for cross-border EUR/GBP/CHF transactions."
  },
  {
    "id": "int_008",
    "title": "Delta Lake DML: MERGE INTO, UPDATE, DELETE",
    "level": "intermediate",
    "category": "Delta Lake DML",
    "description": "Delta Lake's ACID DML operations (MERGE, UPDATE, DELETE) enable true data warehouse semantics on the data lake. MERGE INTO is the most powerful — implementing upsert, delete, and conditional insert in a single atomic operation. These replace complex partition-swap workarounds previously needed with plain Parquet.",
    "code": "-- =============================================\n-- Delta Lake DML Operations\n-- EU BFSI — Account & Transaction Management\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- ─────────────────────────────────────────────\n-- 1. UPDATE — Correct account risk tiers\n-- ─────────────────────────────────────────────\nUPDATE silver_accounts\nSET\n    risk_tier           = 'HIGH',\n    risk_review_date    = current_date(),\n    updated_by          = current_user(),\n    updated_ts          = current_timestamp()\nWHERE account_id IN (\n    SELECT DISTINCT account_id\n    FROM silver_aml_alerts\n    WHERE alert_date >= current_date() - INTERVAL 30 DAYS\n      AND alert_severity = 'CRITICAL'\n      AND alert_status = 'CONFIRMED'\n);\n\n-- ─────────────────────────────────────────────\n-- 2. DELETE — GDPR right-to-erasure\n-- ─────────────────────────────────────────────\nDELETE FROM silver_customer_pii\nWHERE customer_id IN (\n    SELECT customer_id\n    FROM gold_erasure_requests\n    WHERE request_status = 'APPROVED'\n      AND approved_date <= current_date()\n);\n\n-- ─────────────────────────────────────────────\n-- 3. MERGE INTO — Full upsert (SCD Type 1)\n-- Scenario: Intra-day account balance refresh\n-- ─────────────────────────────────────────────\nMERGE INTO silver_accounts AS target\nUSING (\n    SELECT\n        account_id,\n        current_balance,\n        available_balance,\n        credit_limit,\n        account_status,\n        last_activity_date,\n        current_timestamp() AS source_ts\n    FROM bronze_account_snapshot\n    WHERE snapshot_date = current_date()\n      AND batch_id = '${latest_batch_id}'\n) AS source\nON target.account_id = source.account_id\n\nWHEN MATCHED AND target.account_status != source.account_status THEN\n    UPDATE SET\n        target.current_balance    = source.current_balance,\n        target.available_balance  = source.available_balance,\n        target.account_status     = source.account_status,\n        target.last_activity_date = source.last_activity_date,\n        target.updated_ts         = source.source_ts\n\nWHEN MATCHED AND (\n    target.current_balance   != source.current_balance  OR\n    target.available_balance != source.available_balance\n) THEN\n    UPDATE SET\n        target.current_balance    = source.current_balance,\n        target.available_balance  = source.available_balance,\n        target.last_activity_date = source.last_activity_date,\n        target.updated_ts         = source.source_ts\n\nWHEN NOT MATCHED THEN\n    INSERT (\n        account_id, current_balance, available_balance,\n        credit_limit, account_status, last_activity_date,\n        created_ts, updated_ts\n    )\n    VALUES (\n        source.account_id, source.current_balance, source.available_balance,\n        source.credit_limit, source.account_status, source.last_activity_date,\n        source.source_ts, source.source_ts\n    )\n\nWHEN NOT MATCHED BY SOURCE AND target.account_status = 'ACTIVE' THEN\n    UPDATE SET target.account_status = 'CLOSED',\n               target.updated_ts = current_timestamp();",
    "notes": [
      "MERGE INTO is atomic — all matched/not-matched clauses execute in a single transaction, never leaving partial state",
      "WHEN NOT MATCHED BY SOURCE (Delta-specific) handles rows in target that have no match in source — enables soft-delete patterns",
      "Multiple WHEN MATCHED clauses are evaluated in order — only the first matching clause executes per row",
      "For large MERGE operations, add /*+ BROADCAST(source) */ hint if source is small; otherwise ensure both sides are partitioned on join key",
      "Delta records every MERGE as a single commit — DESCRIBE HISTORY shows operationParameters with metrics"
    ],
    "use_case": "Intra-day account balance refresh at a European retail bank — MERGE INTO atomically upserts Core Banking account snapshots received every 4 hours, updating existing accounts and inserting new ones, while soft-deleting accounts no longer present in the source feed."
  },
  {
    "id": "int_009",
    "title": "Delta Lake Time Travel: VERSION AS OF / TIMESTAMP AS OF",
    "level": "intermediate",
    "category": "Delta Lake Features",
    "description": "Delta Lake's time travel feature allows querying historical versions of a table using VERSION AS OF or TIMESTAMP AS OF. This enables data auditing, regulatory point-in-time reconstruction, rollback after erroneous DML, and reproducible ML training datasets from a specific snapshot in time.",
    "code": "-- =============================================\n-- Delta Time Travel\n-- EU BFSI — Regulatory Point-in-Time Queries\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- 1. Query by version number\nSELECT COUNT(*), SUM(txn_amount_eur) AS total_eur\nFROM silver_transactions VERSION AS OF 142\nWHERE txn_date = '2024-12-31';\n\n-- 2. Query by timestamp (EOM snapshot for regulatory reporting)\nSELECT\n    account_id,\n    current_balance,\n    available_balance,\n    account_status\nFROM silver_accounts\nTIMESTAMP AS OF '2024-12-31T23:59:59.000+00:00'\nWHERE account_status = 'ACTIVE';\n\n-- 3. Audit: Compare current vs past state\nSELECT\n    curr.account_id,\n    curr.risk_tier                          AS current_risk_tier,\n    hist.risk_tier                          AS risk_tier_90days_ago,\n    curr.credit_limit                       AS current_limit,\n    hist.credit_limit                       AS limit_90days_ago,\n    curr.credit_limit - hist.credit_limit   AS limit_change\nFROM silver_accounts curr\nJOIN silver_accounts TIMESTAMP AS OF\n    DATE_SUB(current_date(), 90) hist\n    ON curr.account_id = hist.account_id\nWHERE curr.risk_tier != hist.risk_tier\n   OR curr.credit_limit != hist.credit_limit\nORDER BY ABS(curr.credit_limit - hist.credit_limit) DESC;\n\n-- 4. Restore table to a previous version (ROLLBACK)\nRESTORE TABLE silver_accounts\nTO VERSION AS OF 98;\n-- Or by timestamp:\n-- RESTORE TABLE silver_accounts TO TIMESTAMP AS OF '2024-12-01 08:00:00';\n\n-- 5. CTAS from historical snapshot\nCREATE OR REPLACE TABLE eu_bfsi_prod.gold.eom_dec2024_account_snapshot\nUSING DELTA\nCOMMENT 'EOM December 2024 account snapshot for COREP reporting'\nAS\nSELECT *\nFROM eu_bfsi_prod.silver.silver_accounts\nTIMESTAMP AS OF '2024-12-31T23:59:00.000+00:00';\n\n-- 6. Show Delta history (audit log)\nDESCRIBE HISTORY silver_transactions\nLIMIT 30;\n-- Returns: version, timestamp, userName, operation, operationParameters, operationMetrics\n\n-- 7. Find version closest to a business event timestamp\nSELECT version, timestamp, operation, operationMetrics\nFROM (\n    DESCRIBE HISTORY eu_bfsi_prod.silver.silver_transactions\n)\nWHERE timestamp <= '2024-12-31T18:00:00.000+00:00'\nORDER BY timestamp DESC\nLIMIT 1;",
    "notes": [
      "Time travel is only available within the retention window — default 30 days, configurable via delta.deletedFileRetentionDuration",
      "RESTORE TABLE is atomic — it adds a new commit pointing to historical files, rather than replaying old operations",
      "VERSION AS OF is cheaper than TIMESTAMP AS OF — the version→file mapping is O(1) while timestamp requires log scanning",
      "Historical snapshots are available even after OPTIMIZE/VACUUM — but VACUUM with retentionHours=0 deletes old files immediately",
      "Use DESCRIBE HISTORY to find exact version numbers before using VERSION AS OF in audits"
    ],
    "use_case": "A European bank's regulatory team reconstructing a point-in-time account position snapshot for an ECB audit — TIMESTAMP AS OF '2024-12-31T23:59:59' retrieves the exact end-of-year state used for COREP capital adequacy reporting, without any manual archiving."
  },
  {
    "id": "int_010",
    "title": "LATERAL VIEW and Subqueries (Scalar, Correlated)",
    "level": "intermediate",
    "category": "Query Structuring",
    "description": "LATERAL VIEW is used to explode complex types (arrays, maps) into virtual columns while keeping the original row context. Scalar and correlated subqueries allow embedding per-row calculations and existence checks that reference the outer query. These are powerful for complex enrichment patterns in financial data pipelines.",
    "code": "-- =============================================\n-- LATERAL VIEW & Subqueries\n-- EU BFSI — Trade & Account Enrichment\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- 1. LATERAL VIEW EXPLODE with alias\nSELECT\n    t.trade_id,\n    t.trade_date,\n    t.counterparty_id,\n    leg.leg_type,\n    leg.notional,\n    leg.currency,\n    leg.start_date,\n    leg.end_date\nFROM silver_trades t\nLATERAL VIEW EXPLODE(t.trade_legs) AS leg\nWHERE leg.currency = 'EUR'\n  AND leg.notional > 10000000;\n\n-- 2. Multiple LATERAL VIEW (cross-product of arrays)\nSELECT\n    account_id,\n    flag_type,\n    flag_value,\n    product_code\nFROM silver_accounts\nLATERAL VIEW EXPLODE(risk_flags)    AS flag_type\nLATERAL VIEW EXPLODE(product_codes) AS product_code\nWHERE flag_type IN ('AML_ALERT', 'SANCTIONS_MATCH');\n\n-- 3. LATERAL VIEW OUTER (keeps rows even if array is empty/null)\nSELECT\n    c.customer_id,\n    c.customer_name,\n    acc.account_id\nFROM silver_customers c\nLATERAL VIEW OUTER EXPLODE(c.linked_account_ids) AS account_id;\n\n-- 4. Scalar subquery — single value per row\nSELECT\n    t.txn_id,\n    t.account_id,\n    t.txn_amount,\n    -- Scalar subquery: account's monthly average for comparison\n    (\n        SELECT ROUND(AVG(txn_amount), 2)\n        FROM silver_transactions inner_t\n        WHERE inner_t.account_id = t.account_id\n          AND YEAR(inner_t.txn_date)  = YEAR(t.txn_date)\n          AND MONTH(inner_t.txn_date) = MONTH(t.txn_date)\n          AND inner_t.txn_id != t.txn_id\n    )                                       AS monthly_avg_excl_self,\n    t.txn_amount - (\n        SELECT ROUND(AVG(txn_amount), 2)\n        FROM silver_transactions inner_t\n        WHERE inner_t.account_id = t.account_id\n    )                                       AS deviation_from_lifetime_avg\nFROM silver_transactions t\nWHERE t.txn_date = current_date() - INTERVAL 1 DAY\n  AND t.txn_type = 'DEBIT'\n  AND t.txn_amount > 50000;\n\n-- 5. Correlated EXISTS subquery — AML alert filter\nSELECT a.account_id, a.customer_id, a.risk_tier\nFROM silver_accounts a\nWHERE EXISTS (\n    SELECT 1\n    FROM silver_transactions t\n    WHERE t.account_id = a.account_id\n      AND t.txn_amount > 500000\n      AND t.txn_date >= current_date() - INTERVAL 7 DAYS\n      AND t.counterparty_country NOT IN ('DE','FR','NL','BE','ES','IT')\n);\n\n-- 6. NOT EXISTS — accounts with no transactions in 12 months (dormant)\nSELECT a.account_id, a.customer_id, a.account_type\nFROM silver_accounts a\nWHERE a.account_status = 'ACTIVE'\n  AND NOT EXISTS (\n    SELECT 1\n    FROM silver_transactions t\n    WHERE t.account_id = a.account_id\n      AND t.txn_date >= current_date() - INTERVAL 365 DAYS\n  );",
    "notes": [
      "LATERAL VIEW OUTER preserves rows where the array/map is NULL or empty — critical for outer join semantics with explode",
      "Correlated scalar subqueries execute once per row — replace with window functions for large datasets to avoid N+1 execution",
      "EXISTS is more efficient than IN with subquery — Spark converts EXISTS to a semi-join internally",
      "Multiple LATERAL VIEW clauses produce a cartesian product of the arrays — ensure this is the intended behavior",
      "Spark 3.x can optimize correlated subqueries through decorrelation — EXPLAIN EXTENDED reveals if optimization fired"
    ],
    "use_case": "AML screening at a European bank — correlated EXISTS subquery identifies accounts with recent large cross-border transactions to non-EU countries, while LATERAL VIEW EXPLODE processes multi-leg structured product trades from the derivatives portfolio system."
  },
  {
    "id": "adv_001",
    "title": "MERGE INTO for SCD Type 2 (Slowly Changing Dimensions)",
    "level": "advanced",
    "category": "Delta Lake Patterns",
    "description": "SCD Type 2 preserves full history by closing old records and inserting new versions when dimension attributes change. Delta's MERGE INTO with multiple WHEN clauses implements SCD2 atomically. This pattern is critical for financial systems where account and customer attribute history must be preserved for regulatory audit trails.",
    "code": "-- =============================================\n-- SCD Type 2 with MERGE INTO\n-- EU BFSI — Customer Risk Profile History\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- Target SCD2 table structure:\n-- CREATE TABLE silver_dim_customer (\n--     surrogate_key   BIGINT GENERATED ALWAYS AS IDENTITY,\n--     customer_id     STRING NOT NULL,\n--     customer_name   STRING,\n--     risk_tier       STRING,\n--     kyc_status      STRING,\n--     pep_flag        BOOLEAN,\n--     nationality     STRING,\n--     effective_from  TIMESTAMP NOT NULL,\n--     effective_to    TIMESTAMP,  -- NULL = current active record\n--     is_current      BOOLEAN NOT NULL,\n--     record_hash     STRING NOT NULL,  -- MD5 of SCD2 attributes\n--     created_ts      TIMESTAMP,\n--     updated_ts      TIMESTAMP\n-- ) USING DELTA\n-- TBLPROPERTIES ('delta.enableChangeDataFeed' = 'true');\n\n-- Stage incoming changes with hash for change detection\nCREATE OR REPLACE TEMPORARY VIEW v_customer_changes AS\nSELECT\n    customer_id,\n    customer_name,\n    risk_tier,\n    kyc_status,\n    pep_flag,\n    nationality,\n    current_timestamp()                                         AS effective_from,\n    MD5(CONCAT_WS('|',\n        COALESCE(risk_tier, ''),\n        COALESCE(kyc_status, ''),\n        COALESCE(CAST(pep_flag AS STRING), ''),\n        COALESCE(nationality, '')\n    ))                                                          AS record_hash\nFROM bronze_customer_updates\nWHERE batch_date = current_date();\n\n-- Step 1: Close existing active records that have changed\nMERGE INTO silver_dim_customer AS target\nUSING (\n    SELECT\n        src.customer_id,\n        src.record_hash                 AS new_hash,\n        tgt.surrogate_key               AS old_surrogate_key\n    FROM v_customer_changes src\n    JOIN silver_dim_customer tgt\n        ON  src.customer_id = tgt.customer_id\n        AND tgt.is_current = true\n        AND src.record_hash != tgt.record_hash  -- Attribute change detected\n) AS changed_records\nON target.surrogate_key = changed_records.old_surrogate_key\nWHEN MATCHED THEN\n    UPDATE SET\n        target.effective_to = current_timestamp() - INTERVAL 1 MILLISECOND,\n        target.is_current   = false,\n        target.updated_ts   = current_timestamp();\n\n-- Step 2: Insert new versions for changed records + new customers\nMERGE INTO silver_dim_customer AS target\nUSING (\n    -- New versions for changed existing records\n    SELECT\n        src.customer_id,\n        src.customer_name,\n        src.risk_tier,\n        src.kyc_status,\n        src.pep_flag,\n        src.nationality,\n        src.effective_from,\n        NULL                AS effective_to,\n        true                AS is_current,\n        src.record_hash,\n        current_timestamp() AS created_ts,\n        current_timestamp() AS updated_ts\n    FROM v_customer_changes src\n    -- Rows that had existing records but changed\n    WHERE EXISTS (\n        SELECT 1 FROM silver_dim_customer tgt\n        WHERE tgt.customer_id = src.customer_id\n          AND tgt.is_current = false\n          AND tgt.updated_ts >= current_timestamp() - INTERVAL 5 MINUTES\n    )\n    UNION ALL\n    -- Entirely new customers\n    SELECT\n        src.customer_id,\n        src.customer_name,\n        src.risk_tier,\n        src.kyc_status,\n        src.pep_flag,\n        src.nationality,\n        src.effective_from,\n        NULL                AS effective_to,\n        true                AS is_current,\n        src.record_hash,\n        current_timestamp() AS created_ts,\n        current_timestamp() AS updated_ts\n    FROM v_customer_changes src\n    WHERE NOT EXISTS (\n        SELECT 1 FROM silver_dim_customer tgt\n        WHERE tgt.customer_id = src.customer_id\n    )\n) AS new_records\nON 1 = 0  -- Never match — always INSERT\nWHEN NOT MATCHED THEN\n    INSERT (\n        customer_id, customer_name, risk_tier, kyc_status,\n        pep_flag, nationality, effective_from, effective_to,\n        is_current, record_hash, created_ts, updated_ts\n    )\n    VALUES (\n        new_records.customer_id, new_records.customer_name,\n        new_records.risk_tier, new_records.kyc_status,\n        new_records.pep_flag, new_records.nationality,\n        new_records.effective_from, new_records.effective_to,\n        new_records.is_current, new_records.record_hash,\n        new_records.created_ts, new_records.updated_ts\n    );",
    "notes": [
      "Two-step MERGE is required for SCD2: Step 1 closes old records, Step 2 inserts new versions — keeps logic clear",
      "MD5/SHA hash on SCD2 attributes enables efficient change detection without comparing every column individually",
      "ON 1 = 0 is the 'insert-only MERGE' pattern — forces all rows to go to the WHEN NOT MATCHED branch",
      "is_current flag + NULL effective_to identifies current records; always index/cluster on (customer_id, is_current)",
      "Consider GENERATED ALWAYS AS IDENTITY for surrogate keys — Delta natively supports identity columns since DBR 10.4"
    ],
    "use_case": "Customer risk profile history at a European private bank — SCD2 preserves complete audit trail of risk tier changes required by MiFID II suitability assessments, allowing retrospective analysis of whether a customer was classified correctly at the time of each investment recommendation."
  },
  {
    "id": "adv_002",
    "title": "OPTIMIZE and Z-ORDER BY for Delta Performance",
    "level": "advanced",
    "category": "Delta Lake Optimization",
    "description": "OPTIMIZE compacts small Delta files into optimal-sized Parquet files (typically 1GB target), dramatically improving query scan performance. Z-ORDER BY co-locates data with similar values of specified columns within files, enabling data skipping. Together they are the primary performance tuning levers for Delta tables in Databricks.",
    "code": "-- =============================================\n-- OPTIMIZE & Z-ORDER — Delta Table Tuning\n-- EU BFSI — Transaction Table Optimization\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- 1. Basic OPTIMIZE — compact small files\nOPTIMIZE silver_transactions;\n\n-- 2. OPTIMIZE with Z-ORDER (co-locate data by query columns)\nOPTIMIZE silver_transactions\nZORDER BY (account_id, txn_currency, txn_date);\n-- Tip: Z-ORDER on columns most used in WHERE/JOIN predicates\n\n-- 3. OPTIMIZE a specific partition (incremental — new data only)\nOPTIMIZE silver_transactions\nWHERE txn_date = current_date() - INTERVAL 1 DAY\nZORDER BY (account_id, txn_currency);\n\n-- 4. Check optimization statistics\nDESCRIBE DETAIL silver_transactions;\n-- Shows: numFiles, sizeInBytes, numPartitions, minReaderVersion\n\n-- 5. ANALYZE TABLE — compute statistics for the query optimizer\nANALYZE TABLE silver_transactions COMPUTE STATISTICS;\nANALYZE TABLE silver_transactions COMPUTE STATISTICS FOR COLUMNS\n    account_id, txn_currency, txn_amount, txn_date;\n\n-- 6. Set auto-optimize properties\nALTER TABLE silver_transactions\nSET TBLPROPERTIES (\n    'delta.autoOptimize.optimizeWrite'  = 'true',  -- Compact on write\n    'delta.autoOptimize.autoCompact'    = 'true',  -- Background compaction\n    'delta.targetFileSize'              = '134217728',  -- 128MB target\n    'delta.tuneFileSizesForRewrites'    = 'true'   -- Optimize for MERGE\n);\n\n-- 7. VACUUM — remove old/deleted files (safe to run after OPTIMIZE)\n-- Default retention = 7 days. Never set to 0 in production.\nVACUUM silver_transactions RETAIN 168 HOURS;  -- 7 days\n-- Dry run first:\nVACUUM silver_transactions DRY RUN;           -- Shows files to be deleted\n\n-- 8. Liquid Clustering (DBR 13.3+) — dynamic, incremental clustering\n-- Applied at CREATE time:\n-- CREATE TABLE ... CLUSTER BY (account_id, txn_currency);\n\n-- Add/change cluster keys on existing table:\nALTER TABLE silver_transactions\nCLUSTER BY (account_id, txn_currency, txn_date);\n\n-- Run clustering (equivalent to OPTIMIZE for liquid clustering)\nOPTIMIZE silver_transactions;\n-- Databricks auto-selects Z-ORDER columns from CLUSTER BY definition\n\n-- 9. Bloom filter index for equality predicates\nCREATE BLOOMFILTER INDEX ON TABLE silver_transactions\nFOR COLUMNS (txn_id OPTIONS (fpp=0.01, numItems=1000000000));\n-- fpp = false positive probability; lower = larger index but better filtering",
    "notes": [
      "ZORDER BY significantly improves data skipping — Delta skips entire files when min/max column statistics don't overlap the query predicate",
      "Z-ORDER is most effective on high-cardinality columns used in WHERE clauses; avoid low-cardinality columns (e.g., boolean flags)",
      "Liquid Clustering is the modern replacement for both static partitioning and Z-ORDER — supports incremental re-clustering without full rewrites",
      "VACUUM permanently deletes files — ensure no active time travel queries or streaming checkpoints reference older versions before running",
      "OPTIMIZE is idempotent — run it in production pipelines after bulk loads; use WHERE clause to target only new partitions"
    ],
    "use_case": "Post-load optimization for a European bank's silver transaction table receiving 50M+ daily records — OPTIMIZE with Z-ORDER on (account_id, txn_currency) reduces AML query scan time from 4 minutes to 18 seconds by enabling Delta data skipping to eliminate 94% of files for account-specific queries."
  },
  {
    "id": "adv_003",
    "title": "Delta Change Data Feed (CDF) — SQL",
    "level": "advanced",
    "category": "Delta Lake Features",
    "description": "Delta Change Data Feed (CDF) captures row-level change events (insert, update_preimage, update_postimage, delete) on Delta tables. It enables efficient incremental propagation through Medallion layers, CDC pipelines, and audit logging without full table scans. CDF is enabled per table and queryable via SQL.",
    "code": "-- =============================================\n-- Delta Change Data Feed (CDF)\n-- EU BFSI — Incremental Silver→Gold Propagation\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- 1. Enable CDF on existing table\nALTER TABLE silver_accounts\nSET TBLPROPERTIES ('delta.enableChangeDataFeed' = 'true');\n\n-- 2. Read changes since a specific version\nSELECT\n    _change_type,       -- insert | update_preimage | update_postimage | delete\n    _commit_version,    -- Delta version of the commit\n    _commit_timestamp,  -- Timestamp of the commit\n    account_id,\n    risk_tier,\n    kyc_status,\n    account_status\nFROM table_changes('eu_bfsi_prod.silver.silver_accounts', 150)\nORDER BY _commit_version, _change_type;\n\n-- 3. Read changes between version range\nSELECT *\nFROM table_changes('eu_bfsi_prod.silver.silver_accounts', 100, 200);\n\n-- 4. Read changes since a timestamp\nSELECT *\nFROM table_changes(\n    'eu_bfsi_prod.silver.silver_accounts',\n    '2024-12-01T00:00:00.000+00:00'\n);\n\n-- 5. Practical: Only get UPDATES (before/after pair)\nSELECT\n    pre.account_id,\n    pre.risk_tier         AS old_risk_tier,\n    post.risk_tier        AS new_risk_tier,\n    pre.kyc_status        AS old_kyc_status,\n    post.kyc_status       AS new_kyc_status,\n    post._commit_timestamp AS change_ts\nFROM table_changes('eu_bfsi_prod.silver.silver_accounts',\n                   '2024-12-01T00:00:00.000+00:00') pre\nJOIN table_changes('eu_bfsi_prod.silver.silver_accounts',\n                   '2024-12-01T00:00:00.000+00:00') post\n    ON pre.account_id = post.account_id\n    AND pre._commit_version  = post._commit_version\n    AND pre._change_type  = 'update_preimage'\n    AND post._change_type = 'update_postimage'\nWHERE pre.risk_tier != post.risk_tier;  -- Only risk tier changes\n\n-- 6. CDF for incremental gold layer propagation\nINSERT INTO eu_bfsi_prod.gold.gold_account_risk_changes\nSELECT\n    account_id,\n    _change_type            AS change_type,\n    _commit_version         AS source_version,\n    _commit_timestamp       AS change_ts,\n    risk_tier,\n    kyc_status,\n    current_timestamp()     AS processed_ts\nFROM table_changes(\n    'eu_bfsi_prod.silver.silver_accounts',\n    (\n        SELECT COALESCE(MAX(source_version), 0)\n        FROM eu_bfsi_prod.gold.gold_account_risk_changes\n    )  -- Start from last processed version\n)\nWHERE _change_type IN ('update_postimage', 'insert');",
    "notes": [
      "CDF captures update_preimage (before) and update_postimage (after) for each UPDATE — join them on account_id + _commit_version for before/after comparison",
      "CDF storage overhead is minimal — it stores only the changed rows, not full snapshots",
      "Use _commit_version watermarking pattern (MAX of last processed version) for idempotent incremental loads",
      "CDF works with streaming: spark.readStream.format('delta').option('readChangeFeed', 'true').load(path)",
      "CDF requires minReaderVersion=2 and minWriterVersion=4 — automatically set when enabling CDF"
    ],
    "use_case": "Incremental silver→gold propagation at a European asset management firm — CDF eliminates the need to full-scan the 2-billion-row silver_accounts table nightly; instead the gold layer processes only the ~50,000 daily changed rows, reducing pipeline runtime from 2 hours to 3 minutes."
  },
  {
    "id": "adv_004",
    "title": "Streaming Queries in Spark SQL",
    "level": "advanced",
    "category": "Streaming",
    "description": "Spark Structured Streaming exposes a SQL interface for building real-time pipelines. In Databricks, streaming SQL queries can read from Delta tables, Kafka, or Event Hubs as sources and write to Delta as sinks. Trigger options control micro-batch frequency, enabling near-real-time fraud detection and payment processing.",
    "code": "-- =============================================\n-- Structured Streaming in Spark SQL\n-- EU BFSI — Real-time Payment Fraud Detection\n-- =============================================\n\n-- Note: Full streaming setup done via PySpark API;\n-- SQL cells in Databricks can run streaming queries\n-- using CREATE STREAMING TABLE (DLT) or via SQL API.\n\n-- ── In Databricks SQL (non-DLT) ─────────────────\n-- Use spark.sql() from PySpark notebook:\n\n-- PySpark bootstrap for streaming:\n-- stream_df = (\n--   spark.readStream\n--     .format('delta')\n--     .option('readChangeFeed', 'true')\n--     .option('startingVersion', 'latest')\n--     .table('eu_bfsi_prod.bronze.bronze_transactions')\n-- )\n-- stream_df.createOrReplaceTempView('streaming_transactions')\n\n-- ── Then query with SQL ──────────────────────────\n\n-- 1. Streaming aggregation with watermark (SQL via spark.sql)\n-- SELECT\n--     window(txn_timestamp, '5 minutes').start  AS window_start,\n--     window(txn_timestamp, '5 minutes').end    AS window_end,\n--     account_id,\n--     COUNT(*)                                  AS txn_count_5min,\n--     SUM(txn_amount)                           AS volume_5min,\n--     MAX(txn_amount)                           AS max_txn_5min\n-- FROM streaming_transactions\n-- GROUP BY window(txn_timestamp, '5 minutes'), account_id\n\n-- ── Full PySpark Streaming Example ──────────────\n/*\nfrom pyspark.sql import functions as F\n\n# Read stream from Delta CDF\nstream = (\n    spark.readStream\n        .format('delta')\n        .option('readChangeFeed', 'true')\n        .option('startingTimestamp', '2024-01-01')\n        .table('eu_bfsi_prod.bronze.bronze_transactions')\n        .where(\"_change_type = 'insert'\")\n)\n\n# Apply fraud detection aggregation\nfraud_alerts = (\n    stream\n    .withWatermark('txn_timestamp', '10 minutes')\n    .groupBy(\n        F.window('txn_timestamp', '5 minutes'),\n        'account_id'\n    )\n    .agg(\n        F.count('*').alias('txn_count'),\n        F.sum('txn_amount').alias('total_amount'),\n        F.collect_set('counterparty_country').alias('countries')\n    )\n    .where('txn_count > 10 OR total_amount > 100000')\n)\n\n# Write to Delta alert table\nquery = (\n    fraud_alerts.writeStream\n        .format('delta')\n        .outputMode('append')\n        .option('checkpointLocation',\n                'abfss://checkpoints@euadlsprod.dfs.core.windows.net/fraud_stream/')\n        .trigger(processingTime='30 seconds')\n        .toTable('eu_bfsi_prod.gold.gold_realtime_fraud_alerts')\n)\n*/\n\n-- ── Foreground streaming Delta table query ───────\n-- After streaming writes, query the output table normally:\nSELECT\n    window_start,\n    window_end,\n    account_id,\n    txn_count_5min,\n    volume_5min,\n    SIZE(countries)         AS unique_countries,\n    CASE\n        WHEN txn_count_5min > 50  THEN 'HIGH_VELOCITY'\n        WHEN volume_5min > 500000 THEN 'HIGH_VALUE'\n        ELSE 'STANDARD'\n    END                     AS alert_type,\n    current_timestamp()     AS alerted_ts\nFROM eu_bfsi_prod.gold.gold_realtime_fraud_alerts\nWHERE window_start >= current_timestamp() - INTERVAL 1 HOUR\nORDER BY volume_5min DESC;",
    "notes": [
      "Watermarks define how late data is tolerated before a streaming window closes — critical for out-of-order payment events",
      "Trigger(processingTime='30 seconds') creates micro-batches; Trigger(once=True) processes all available data once (batch-streaming hybrid)",
      "Checkpoints store streaming state — always use ADLS Gen2 paths for fault-tolerant, resumable streams in Azure Databricks",
      "Delta's exactly-once semantics with idempotent writes ensure no duplicate alerts even after stream failures and restarts",
      "Use Delta Live Tables (DLT) for production streaming — it manages checkpoints, restarts, and monitoring automatically"
    ],
    "use_case": "Real-time payment fraud detection at a European payments processor — 5-minute tumbling windows on transaction streams detect velocity attacks (>10 transactions in 5 minutes) and high-value anomalies, triggering fraud alerts to Visa/Mastercard within 30 seconds of suspicious activity."
  },
  {
    "id": "adv_005",
    "title": "Schema Enforcement and Schema Evolution in Delta",
    "level": "advanced",
    "category": "Delta Lake Features",
    "description": "Delta Lake enforces schema on write by default, rejecting data that doesn't match the table schema. Schema evolution allows controlled schema changes (adding columns, widening types) via configuration or DDL. Understanding when to enforce vs evolve schemas is critical for data quality and pipeline reliability in BFSI environments.",
    "code": "-- =============================================\n-- Schema Enforcement & Evolution\n-- EU BFSI — Handling Source Schema Changes\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- 1. Schema enforcement is ON by default\n-- This INSERT will FAIL if source has extra/missing columns:\n-- INSERT INTO silver_transactions SELECT * FROM bronze_with_new_cols;\n-- Error: A schema mismatch detected when writing to the Delta table\n\n-- 2. Enable schema evolution for MERGE/INSERT\n-- Option A: Session-level config\nSET spark.databricks.delta.schema.autoMerge.enabled = true;\n\n-- 3. Add columns via ALTER TABLE (preferred explicit approach)\nALTER TABLE silver_transactions\n    ADD COLUMN iban_sender  STRING COMMENT 'IBAN of sending account (SEPA mandate)',\n    ADD COLUMN iban_receiver STRING COMMENT 'IBAN of receiving account (SEPA mandate)',\n    ADD COLUMN sepa_mandate_ref STRING COMMENT 'SEPA direct debit mandate reference';\n\n-- 4. Rename column (requires columnMapping mode = name)\nALTER TABLE silver_transactions\n    RENAME COLUMN txn_status TO transaction_status;\n\n-- 5. Drop column (soft-drop with column mapping)\nALTER TABLE silver_transactions\n    DROP COLUMN legacy_batch_code;\n-- Note: Data is NOT physically removed — column mapping hides it\n-- Physical removal requires REORG TABLE ... APPLY (PURGE)\n\nREORG TABLE silver_transactions APPLY (PURGE);\n-- This rewrites Parquet files without dropped columns\n\n-- 6. Change column comment\nALTER TABLE silver_transactions\n    ALTER COLUMN txn_amount\n    COMMENT 'Transaction amount in originating currency — DECIMAL(18,4)';\n\n-- 7. Column mapping modes\nALTER TABLE silver_transactions\nSET TBLPROPERTIES (\n    'delta.columnMapping.mode' = 'name',  -- Enables rename/drop\n    'delta.minReaderVersion'   = '2',\n    'delta.minWriterVersion'   = '5'\n);\n\n-- 8. Schema evolution in MERGE (with autoMerge enabled)\n-- Source has new column 'sepa_mandate_ref' not in target:\nMERGE INTO silver_transactions AS t\nUSING bronze_transactions_v2 AS s\nON t.txn_id = s.txn_id\nWHEN MATCHED THEN UPDATE SET *  -- Includes new columns if autoMerge enabled\nWHEN NOT MATCHED THEN INSERT *;  -- Inserts all source columns\n\n-- 9. Type widening (DBR 14.1+)\nALTER TABLE silver_transactions\n    ALTER COLUMN processing_fee TYPE DECIMAL(18,6);\n-- Widening DECIMAL(10,4) → DECIMAL(18,6) is safe\n-- Narrowing (18,6 → 10,4) is NOT supported\n\n-- 10. Check current schema evolution settings\nSHOW TBLPROPERTIES silver_transactions;",
    "notes": [
      "Schema enforcement protects data quality — never disable autoMerge globally; enable per-session or per-table carefully",
      "Column mapping (mode=name) is required for RENAME and DROP column operations — enables physical-logical column name decoupling",
      "REORG TABLE ... APPLY (PURGE) physically rewrites files to remove dropped columns — needed for GDPR column-level erasure",
      "Type widening (INT→BIGINT, DECIMAL scale increase) is safe; type narrowing is never allowed in Delta",
      "Schema evolution in MERGE with * wildcard is powerful but risky — new source columns silently propagate to target; validate source schema changes explicitly"
    ],
    "use_case": "Handling SWIFT ISO 20022 schema migration at a European correspondent bank — the message format added 15 new optional fields in the 2024 standard update. Delta column mapping allows adding new columns without table rewrites, while existing consumers continue reading the old schema without code changes."
  },
  {
    "id": "adv_006",
    "title": "External Tables vs Managed Tables and Hive vs Unity Catalog",
    "level": "advanced",
    "category": "Metadata & Catalog",
    "description": "Understanding the distinction between managed and external tables, and between Hive Metastore and Unity Catalog, is fundamental for enterprise data governance. Each has different behavior on DROP TABLE, storage ownership, and access control. Unity Catalog adds catalog-level governance, lineage, and fine-grained access controls not available in Hive.",
    "code": "-- =============================================\n-- Managed vs External Tables\n-- Hive Metastore vs Unity Catalog\n-- EU BFSI — Data Governance Architecture\n-- =============================================\n\n-- ── MANAGED TABLE (Unity Catalog manages storage) ──\nCREATE TABLE IF NOT EXISTS eu_bfsi_prod.silver.silver_transactions\n(\n    txn_id      STRING NOT NULL,\n    txn_amount  DECIMAL(18,4),\n    txn_date    DATE\n)\nUSING DELTA\nCOMMENT 'Managed table — UC owns storage lifecycle';\n-- Storage: auto-assigned under schema MANAGED LOCATION\n-- DROP TABLE: deletes BOTH metadata AND data files !\n\n-- ── EXTERNAL TABLE (you own the storage) ──────────\nCREATE TABLE IF NOT EXISTS eu_bfsi_prod.bronze.bronze_swift_messages\nUSING DELTA\nLOCATION 'abfss://bronze@euadlsprod.dfs.core.windows.net/swift/'\nCOMMENT 'External table — ADLS path controlled independently';\n-- DROP TABLE: deletes ONLY metadata. Data files remain.\n\n-- ── Convert external to managed (via CLONE) ───────\nCREATE TABLE eu_bfsi_prod.silver.silver_swift_messages\nDEEP CLONE eu_bfsi_prod.bronze.bronze_swift_messages;\n\n-- ── Hive Metastore table (legacy) ─────────────────\n-- USE CATALOG hive_metastore;\n-- USE SCHEMA default;\n-- CREATE TABLE hms_transactions USING DELTA LOCATION '/mnt/silver/transactions/';\n-- Note: No catalog-level governance, no column-level security,\n--       no automated lineage in Hive Metastore\n\n-- ── Unity Catalog migration pattern ───────────────\n-- SYNC from Hive Metastore to UC using SYNC command:\nSYNC SCHEMA eu_bfsi_prod.silver\nFROM hive_metastore.eu_bfsi_silver\nDRY RUN;  -- Preview before executing\n\n-- ── Unity Catalog governance features ─────────────\n-- 1. Table owner\nALTER TABLE eu_bfsi_prod.silver.silver_transactions\n    SET OWNER TO `data-engineering-team`;\n\n-- 2. Column-level comment for data catalog\nALTER TABLE eu_bfsi_prod.silver.silver_transactions\n    ALTER COLUMN txn_amount\n    COMMENT 'Transaction monetary amount; classified as PII-adjacent financial data';\n\n-- 3. Table tags (Unity Catalog)\nALTER TABLE eu_bfsi_prod.silver.silver_transactions\n    SET TAGS ('data_classification' = 'CONFIDENTIAL',\n              'data_domain' = 'payments',\n              'retention_years' = '7',\n              'gdpr_relevant' = 'true');\n\n-- 4. View table lineage (via UI or API)\n-- SELECT * FROM system.access.table_lineage\n-- WHERE target_table_full_name = 'eu_bfsi_prod.silver.silver_transactions';\n\n-- 5. Cross-catalog query (Unity Catalog)\nSELECT\n    a.account_id,\n    a.risk_tier,\n    b.txn_count,\n    b.total_amount_eur\nFROM eu_bfsi_prod.silver.silver_accounts a\nJOIN eu_bfsi_analytics.gold.gold_account_metrics b\n    ON a.account_id = b.account_id\nWHERE a.risk_tier = 'HIGH';",
    "notes": [
      "DROP on managed table = data loss; DROP on external table = metadata only — this difference is critical for production governance",
      "Unity Catalog's three-level namespace (catalog.schema.table) enables cross-workspace and cross-environment queries",
      "External tables are preferred when data is shared with non-Databricks consumers (ADF, Synapse, Fabric) or when storage lifecycle is managed independently",
      "Unity Catalog provides automated data lineage — column-level lineage tracks transformation across notebooks, jobs, and SQL queries",
      "Hive Metastore is workspace-scoped; Unity Catalog is account-scoped — enabling cross-workspace governance from a single control plane"
    ],
    "use_case": "Migrating a European bank's Databricks workspace from Hive Metastore to Unity Catalog — external Delta tables are preserved during migration (data untouched), while Unity Catalog's GRANT/REVOKE and column masking replace the legacy IP-based access controls that couldn't scale to 400+ data consumers across 12 EU jurisdictions."
  },
  {
    "id": "adv_007",
    "title": "EXPLAIN EXTENDED and Query Hints",
    "level": "advanced",
    "category": "Query Optimization",
    "description": "EXPLAIN EXTENDED reveals the full logical, analyzed, optimized, and physical execution plan for a Spark SQL query. Query hints allow engineers to override Spark's optimizer decisions for joins and aggregations. These tools are essential for diagnosing performance issues in complex BFSI pipelines where the optimizer makes suboptimal choices.",
    "code": "-- =============================================\n-- EXPLAIN & Query Hints — Performance Tuning\n-- EU BFSI — Optimizing COREP Report Queries\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA gold;\n\n-- 1. EXPLAIN — show physical plan only\nEXPLAIN\nSELECT account_id, SUM(txn_amount_eur)\nFROM silver_transactions\nWHERE txn_date = '2024-12-31'\nGROUP BY account_id;\n\n-- 2. EXPLAIN EXTENDED — full plan (logical, analyzed, optimized, physical)\nEXPLAIN EXTENDED\nSELECT\n    t.account_id,\n    a.risk_tier,\n    SUM(t.txn_amount_eur) AS total_eur\nFROM silver_transactions t\nJOIN silver_accounts a ON t.account_id = a.account_id\nWHERE t.txn_date BETWEEN '2024-01-01' AND '2024-12-31'\nGROUP BY t.account_id, a.risk_tier;\n-- Look for: FileScan (partition pruning), BroadcastHashJoin vs SortMergeJoin\n\n-- 3. EXPLAIN FORMATTED — structured output for parsing\nEXPLAIN FORMATTED\nSELECT * FROM silver_transactions WHERE account_id = 'ACC-123456';\n\n-- 4. EXPLAIN COST — includes estimated row counts and sizes\nEXPLAIN COST\nSELECT account_id, COUNT(*) FROM silver_transactions GROUP BY account_id;\n\n-- ─────────────────────────────────────────────\n-- QUERY HINTS\n-- ─────────────────────────────────────────────\n\n-- 5. BROADCAST hint — force small table broadcast\nSELECT /*+ BROADCAST(a) */\n    t.txn_id,\n    t.txn_amount,\n    a.account_type,\n    a.risk_tier\nFROM silver_transactions t\nJOIN silver_accounts a ON t.account_id = a.account_id\nWHERE t.txn_date = current_date();\n-- Use when: silver_accounts < 10MB (small dim), auto-broadcast not triggered\n\n-- 6. MERGE join hint — force sort-merge join\nSELECT /*+ MERGE(t, r) */\n    t.txn_id,\n    r.regulation_code\nFROM silver_transactions t\nJOIN dim_regulation_mappings r\n    ON t.txn_amount BETWEEN r.min_amount AND r.max_amount\nWHERE t.txn_date = current_date();\n\n-- 7. SHUFFLE_HASH hint — use shuffle hash join\nSELECT /*+ SHUFFLE_HASH(t) */\n    t.account_id,\n    s.statement_month,\n    SUM(t.txn_amount) AS total\nFROM silver_transactions t\nJOIN silver_statements s ON t.account_id = s.account_id\nGROUP BY t.account_id, s.statement_month;\n\n-- 8. COALESCE / REPARTITION hints\nSELECT /*+ REPARTITION(50, account_id) */\n    account_id,\n    txn_currency,\n    SUM(txn_amount_eur) AS total\nFROM silver_transactions\nWHERE txn_date >= '2024-01-01'\nGROUP BY account_id, txn_currency;\n\n-- 9. REBALANCE — adaptive output file sizing (Databricks)\nSELECT /*+ REBALANCE(10) */\n    *\nFROM silver_transactions\nWHERE txn_date = current_date();\n\n-- 10. Skew join hint — handle data skew\nSELECT /*+ SKEW('t', 'account_id', ('HIGH_VOLUME_ACCT_001', 'HIGH_VOLUME_ACCT_002')) */\n    t.txn_id,\n    t.txn_amount,\n    a.risk_tier\nFROM silver_transactions t\nJOIN silver_accounts a ON t.account_id = a.account_id;",
    "notes": [
      "Look for 'BroadcastHashJoin' vs 'SortMergeJoin' in EXPLAIN output — SortMergeJoin on small tables indicates auto-broadcast threshold needs raising",
      "EXPLAIN EXTENDED shows 4 plans: Parsed → Analyzed → Optimized → Physical; the Physical plan is what actually runs",
      "BROADCAST hint is the most commonly needed — add it when joining a large fact table to a small dimension where auto-broadcast fails",
      "SKEW hint splits skewed partition keys into sub-partitions — critical for accounts with millions of transactions causing one executor to stall",
      "AQE (Adaptive Query Execution) handles many cases automatically in Spark 3.x — use EXPLAIN to verify AQE optimizations fired before adding hints"
    ],
    "use_case": "A TCS data engineer debugging a COREP regulatory report query that's taking 45 minutes due to a SortMergeJoin on a 500MB accounts dimension table — EXPLAIN EXTENDED reveals the auto-broadcast threshold was breached by 2MB; adding BROADCAST(a) hint drops runtime to 4 minutes."
  },
  {
    "id": "adv_008",
    "title": "Generated Columns, Column Masking, and TBLPROPERTIES",
    "level": "advanced",
    "category": "Delta Lake Features",
    "description": "Generated columns are computed automatically from other columns on write, enabling derived partition keys without application changes. Column masking applies dynamic data redaction based on the querying user's permissions — critical for PII/GDPR compliance in BFSI environments. TBLPROPERTIES store feature flags and custom metadata.",
    "code": "-- =============================================\n-- Generated Columns, Column Masking, TBLPROPERTIES\n-- EU BFSI — PII-Safe Transaction Analytics\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- 1. Generated columns — auto-derived on INSERT\nCREATE TABLE IF NOT EXISTS silver_transactions_v2 (\n    txn_id              STRING          NOT NULL,\n    account_id          STRING          NOT NULL,\n    txn_amount          DECIMAL(18, 4)  NOT NULL,\n    txn_currency        STRING(3)       NOT NULL,\n    txn_timestamp       TIMESTAMP       NOT NULL,\n\n    -- Generated columns (computed from other columns)\n    txn_date            DATE            GENERATED ALWAYS AS (DATE(txn_timestamp))\n                        COMMENT 'Derived partition key from txn_timestamp',\n\n    txn_year_month      STRING          GENERATED ALWAYS AS\n                        (DATE_FORMAT(txn_timestamp, 'yyyy-MM'))\n                        COMMENT 'Year-month label for reporting',\n\n    amount_eur_band     STRING          GENERATED ALWAYS AS (\n                        CASE\n                            WHEN txn_amount <  1000   THEN 'MICRO'\n                            WHEN txn_amount <  10000  THEN 'SMALL'\n                            WHEN txn_amount <  100000 THEN 'MEDIUM'\n                            WHEN txn_amount <  1000000 THEN 'LARGE'\n                            ELSE                          'WHALE'\n                        END)\n                        COMMENT 'Amount band for regulatory bucketing',\n\n    is_high_value       BOOLEAN         GENERATED ALWAYS AS (txn_amount >= 15000)\n                        COMMENT 'CTR threshold flag (EU: 15,000 EUR)'\n)\nUSING DELTA\nPARTITIONED BY (txn_date)  -- Generated column used as partition key!\nCOMMENT 'Silver transactions with generated columns';\n\n-- Insert works without specifying generated columns\nINSERT INTO silver_transactions_v2 (txn_id, account_id, txn_amount, txn_currency, txn_timestamp)\nVALUES ('TXN-001', 'ACC-001', 25000.00, 'EUR', '2024-12-15T14:30:00');\n-- txn_date, txn_year_month, amount_eur_band, is_high_value are auto-computed\n\n-- 2. Column Masking (Unity Catalog — DBR 12.2+)\n-- Create a masking function\nCREATE FUNCTION IF NOT EXISTS eu_bfsi_prod.security.mask_iban (\n    iban STRING\n)\nRETURNS STRING\nRETURN CASE\n    WHEN is_member('data-analysts-gdpr')   THEN '****' || RIGHT(iban, 4)\n    WHEN is_member('compliance-officers')  THEN LEFT(iban, 4) || '****' || RIGHT(iban, 4)\n    ELSE iban  -- Full access for privileged users\nEND;\n\n-- Apply mask to column\nALTER TABLE eu_bfsi_prod.silver.silver_accounts\n    ALTER COLUMN iban\n    SET MASK eu_bfsi_prod.security.mask_iban;\n\n-- Unset mask\n-- ALTER TABLE eu_bfsi_prod.silver.silver_accounts\n--     ALTER COLUMN iban UNSET MASK;\n\n-- 3. TBLPROPERTIES — feature flags and custom metadata\nALTER TABLE silver_transactions\nSET TBLPROPERTIES (\n    -- Delta feature flags\n    'delta.enableChangeDataFeed'              = 'true',\n    'delta.autoOptimize.optimizeWrite'        = 'true',\n    'delta.autoOptimize.autoCompact'          = 'true',\n    'delta.columnMapping.mode'                = 'name',\n    'delta.enableDeletionVectors'             = 'true',  -- Faster DELETEs\n    'delta.enableRowTracking'                 = 'true',  -- Row-level provenance\n    -- Custom business metadata\n    'data.owner'                              = 'payments-engineering@bank.eu',\n    'data.classification'                     = 'CONFIDENTIAL',\n    'data.gdpr'                               = 'true',\n    'data.retention.years'                    = '7',\n    'data.pii.columns'                        = 'account_id,iban_sender,iban_receiver',\n    'pipeline.source'                         = 'AUTO_LOADER_V2',\n    'pipeline.sla.hours'                      = '4',\n    'business.domain'                         = 'PAYMENTS',\n    'business.regulation'                     = 'PSD2,GDPR,AML5'\n);",
    "notes": [
      "Generated columns are enforced on write — if you INSERT a different value for a generated column, Spark throws an error",
      "Generated columns as partition keys eliminate the need for application code to extract partition values from timestamps",
      "Column masking is evaluated at query time per user/group — zero data movement, pure access control layer",
      "is_member() in masking functions checks Unity Catalog group membership dynamically at query runtime",
      "Deletion Vectors (TBLPROPERTIES 'delta.enableDeletionVectors'='true') make DELETE/UPDATE operations 10-100x faster by using soft-delete markers instead of file rewrites"
    ],
    "use_case": "GDPR-compliant transaction analytics platform at a European bank — generated columns automatically derive CTR flags and amount bands from raw amounts, while column masking ensures IBAN numbers shown to analysts are redacted to last-4-digits, with full values visible only to compliance officers with the appropriate Unity Catalog group membership."
  },
  {
    "id": "arc_001",
    "title": "Full Medallion Architecture DDL in Spark SQL",
    "level": "architect",
    "category": "Architecture Patterns",
    "description": "The Medallion Architecture (Bronze → Silver → Gold) organizes data into progressively refined layers. Bronze captures raw data with minimal transformation, Silver cleanses and conforms, Gold delivers business-ready aggregations. Implementing this in SQL requires careful DDL design with proper partitioning, clustering, properties, and governance from the start.",
    "code": "-- =====================================================================\n-- FULL MEDALLION ARCHITECTURE DDL\n-- EU BFSI — Pan-European Retail Bank Transaction Platform\n-- Catalog: eu_bfsi_prod | Workspace: Azure Databricks (East US 2)\n-- Author: Data Engineering Team | Santosh Jammi, TCS\n-- =====================================================================\n\n-- ── CATALOG & SCHEMA SETUP ────────────────────────────────────────────\nCREATE CATALOG IF NOT EXISTS eu_bfsi_prod\n  COMMENT 'Production catalog — EU Banking platform on Azure Databricks';\n\nCREATE SCHEMA IF NOT EXISTS eu_bfsi_prod.bronze\n  MANAGED LOCATION 'abfss://bronze@euadlsprod.dfs.core.windows.net/'\n  COMMENT 'Raw ingestion — no business transformations';\n\nCREATE SCHEMA IF NOT EXISTS eu_bfsi_prod.silver\n  MANAGED LOCATION 'abfss://silver@euadlsprod.dfs.core.windows.net/'\n  COMMENT 'Cleansed, conformed, enriched';\n\nCREATE SCHEMA IF NOT EXISTS eu_bfsi_prod.gold\n  MANAGED LOCATION 'abfss://gold@euadlsprod.dfs.core.windows.net/'\n  COMMENT 'Aggregated, business-ready, BI/ML ready';\n\n-- ── BRONZE LAYER ───────────────────────────────────────────────────────\nCREATE TABLE IF NOT EXISTS eu_bfsi_prod.bronze.bronze_transactions (\n    _ingest_id          STRING          COMMENT 'Auto Loader file UUID',\n    _source_file        STRING          COMMENT 'ADLS file path',\n    _source_ts          TIMESTAMP       COMMENT 'File modification time',\n    _ingest_ts          TIMESTAMP       NOT NULL COMMENT 'Databricks ingest time',\n    _batch_id           STRING          COMMENT 'Pipeline run ID',\n    _rescued_data       STRING          COMMENT 'Auto Loader schema rescue',\n    txn_id              STRING,\n    account_id          STRING,\n    txn_raw_amount      STRING          COMMENT 'Raw — may contain commas/currency symbols',\n    txn_currency        STRING,\n    txn_type            STRING,\n    txn_status          STRING,\n    txn_timestamp_raw   STRING          COMMENT 'Raw timestamp string from source',\n    source_system       STRING          COMMENT 'CORE_BANKING | SWIFT | SEPA | PSD2_OPEN'\n)\nUSING DELTA\nPARTITIONED BY (ingest_date DATE GENERATED ALWAYS AS (DATE(_ingest_ts)))\nCOMMENT 'Bronze — raw transactions as landed from source systems'\nTBLPROPERTIES (\n    'delta.enableChangeDataFeed'       = 'true',\n    'delta.autoOptimize.optimizeWrite' = 'true',\n    'delta.columnMapping.mode'         = 'name',\n    'delta.enableDeletionVectors'      = 'true',\n    'data.layer'                       = 'bronze',\n    'data.classification'              = 'RAW_CONFIDENTIAL',\n    'pipeline.source'                  = 'AUTO_LOADER',\n    'data.retention.years'             = '7'\n);\n\n-- ── SILVER LAYER ───────────────────────────────────────────────────────\nCREATE TABLE IF NOT EXISTS eu_bfsi_prod.silver.silver_transactions (\n    txn_id              STRING          NOT NULL COMMENT 'Deduplicated transaction key',\n    account_id          STRING          NOT NULL,\n    counterparty_id     STRING,\n    txn_amount          DECIMAL(18, 4)  NOT NULL,\n    txn_currency        STRING(3)       NOT NULL,\n    txn_amount_eur      DECIMAL(18, 4)  COMMENT 'Normalized EUR amount via ECB rate',\n    fx_rate             DECIMAL(12, 8),\n    txn_type            STRING,\n    txn_status          STRING,\n    txn_timestamp       TIMESTAMP       NOT NULL,\n    txn_date            DATE            GENERATED ALWAYS AS (DATE(txn_timestamp)),\n    value_date          DATE,\n    source_system       STRING,\n    is_high_value       BOOLEAN         GENERATED ALWAYS AS (txn_amount >= 15000),\n    amount_band         STRING          GENERATED ALWAYS AS (\n                        CASE\n                            WHEN txn_amount < 1000    THEN 'MICRO'\n                            WHEN txn_amount < 10000   THEN 'SMALL'\n                            WHEN txn_amount < 100000  THEN 'MEDIUM'\n                            WHEN txn_amount < 1000000 THEN 'LARGE'\n                            ELSE 'WHALE'\n                        END),\n    dq_score            DECIMAL(5, 2)   COMMENT 'Data quality score 0-100',\n    dq_flags            ARRAY<STRING>   COMMENT 'DQ rule violations',\n    _bronze_ingest_ts   TIMESTAMP       COMMENT 'Source bronze ingest time',\n    _silver_ts          TIMESTAMP       NOT NULL COMMENT 'Silver promotion timestamp'\n)\nUSING DELTA\nPARTITIONED BY (txn_date)\nCLUSTER BY (account_id, txn_currency)\nCOMMENT 'Silver — cleansed, deduplicated, FX-normalized transactions'\nTBLPROPERTIES (\n    'delta.enableChangeDataFeed'            = 'true',\n    'delta.autoOptimize.optimizeWrite'      = 'true',\n    'delta.autoOptimize.autoCompact'        = 'true',\n    'delta.columnMapping.mode'              = 'name',\n    'delta.enableDeletionVectors'           = 'true',\n    'delta.enableRowTracking'               = 'true',\n    'data.layer'                            = 'silver',\n    'data.classification'                   = 'CONFIDENTIAL',\n    'data.pii.columns'                      = 'account_id,counterparty_id',\n    'data.gdpr'                             = 'true',\n    'data.retention.years'                  = '7'\n);\n\n-- ── GOLD LAYER ─────────────────────────────────────────────────────────\nCREATE TABLE IF NOT EXISTS eu_bfsi_prod.gold.gold_daily_account_summary (\n    snapshot_date       DATE            NOT NULL,\n    account_id          STRING          NOT NULL,\n    risk_tier           STRING,\n    product_code        STRING,\n    country_code        STRING,\n    txn_count           BIGINT,\n    debit_count         BIGINT,\n    credit_count        BIGINT,\n    total_debit_eur     DECIMAL(18, 2),\n    total_credit_eur    DECIMAL(18, 2),\n    net_flow_eur        DECIMAL(18, 2),\n    max_single_txn_eur  DECIMAL(18, 2),\n    avg_txn_eur         DECIMAL(18, 2),\n    unique_currencies   BIGINT,\n    unique_counterparties BIGINT,\n    high_value_count    BIGINT          COMMENT 'Transactions >= 15,000 EUR (CTR threshold)',\n    cross_border_count  BIGINT,\n    dq_pass_rate        DECIMAL(5, 2)   COMMENT 'Data quality pass % for the day',\n    _gold_ts            TIMESTAMP       NOT NULL COMMENT 'Gold layer computation timestamp'\n)\nUSING DELTA\nPARTITIONED BY (snapshot_date)\nCOMMAND 'Gold — daily account-level summary for regulatory & BI reporting'\nTBLPROPERTIES (\n    'delta.autoOptimize.optimizeWrite'  = 'true',\n    'delta.autoOptimize.autoCompact'    = 'true',\n    'data.layer'                        = 'gold',\n    'data.classification'               = 'INTERNAL',\n    'data.retention.years'              = '10',\n    'business.domain'                   = 'PAYMENTS_ANALYTICS',\n    'business.consumers'                = 'RISK,COMPLIANCE,FINANCE,BI'\n);",
    "notes": [
      "Bronze = raw as-is with ingestion metadata; Silver = validated, typed, enriched; Gold = aggregated, business-ready — each layer has a distinct contract",
      "Generated columns (txn_date, is_high_value, amount_band) eliminate application-layer logic errors and guarantee consistency",
      "CLUSTER BY (Liquid Clustering) in Silver enables incremental re-clustering as data grows — no need to repartition",
      "CDF enabled on Bronze and Silver enables efficient incremental propagation — avoid full scans on multi-billion row tables",
      "TBLPROPERTIES as a data catalog: store owner, classification, PII columns, GDPR flag, retention, and business domain — queryable via SHOW TBLPROPERTIES"
    ],
    "use_case": "Greenfield Medallion Architecture for a pan-European retail bank's Azure Databricks platform — supporting 15M daily transactions across 12 EU countries, with full lineage from Core Banking raw events through to COREP/FINREP regulatory reports and Power BI dashboards, all governed by Unity Catalog."
  },
  {
    "id": "arc_002",
    "title": "Delta Live Tables (DLT) SQL Syntax — Multi-hop Pipeline",
    "level": "architect",
    "category": "Delta Live Tables",
    "description": "Delta Live Tables (DLT) is Databricks' declarative ETL framework. SQL-based DLT pipelines define streaming and batch tables with expectations (data quality rules) and automatic dependency resolution. DLT manages infrastructure, retries, monitoring, and schema evolution, making it ideal for production Medallion pipelines.",
    "code": "-- =============================================\n-- Delta Live Tables (DLT) SQL Pipeline\n-- EU BFSI — Bronze→Silver→Gold Multi-hop\n-- =============================================\n-- File: /pipeline/eu_bfsi_dlt_pipeline.sql\n-- Pipeline name: EU_BFSI_TXN_PIPELINE\n-- =============================================\n\n-- ─── BRONZE: Raw Ingestion via Auto Loader ────\nCREATE OR REFRESH STREAMING TABLE bronze_transactions\nCOMMENT 'Bronze — raw transactions from ADLS via Auto Loader'\nTBLPROPERTIES (\n    'delta.enableChangeDataFeed' = 'true',\n    'quality.layer'              = 'bronze'\n)\nAS SELECT\n    _metadata.file_path         AS _source_file,\n    _metadata.file_modification_time AS _source_ts,\n    current_timestamp()         AS _ingest_ts,\n    monotonically_increasing_id() AS _row_id,\n    *\nFROM cloud_files(\n    'abfss://raw@euadlsprod.dfs.core.windows.net/transactions/',\n    'json',\n    map(\n        'cloudFiles.schemaLocation',\n        'abfss://checkpoints@euadlsprod.dfs.core.windows.net/bronze_txn_schema/',\n        'cloudFiles.inferColumnTypes',   'true',\n        'cloudFiles.schemaEvolutionMode', 'rescue'\n    )\n);\n\n-- ─── SILVER: Cleansed with DQ Expectations ────\nCREATE OR REFRESH STREAMING TABLE silver_transactions (\n  CONSTRAINT txn_id_not_null\n      EXPECT (txn_id IS NOT NULL) ON VIOLATION DROP ROW,\n  CONSTRAINT valid_amount\n      EXPECT (txn_amount > 0 AND txn_amount < 100000000) ON VIOLATION DROP ROW,\n  CONSTRAINT valid_currency\n      EXPECT (txn_currency RLIKE '^[A-Z]{3}$') ON VIOLATION DROP ROW,\n  CONSTRAINT valid_timestamp\n      EXPECT (txn_timestamp IS NOT NULL AND txn_timestamp <= current_timestamp())\n      ON VIOLATION DROP ROW,\n  CONSTRAINT account_id_not_null\n      EXPECT (account_id IS NOT NULL) ON VIOLATION WARN  -- Warn, don't drop\n)\nCOMMENT 'Silver — validated, typed, FX-normalized transactions'\nTBLPROPERTIES (\n    'delta.enableChangeDataFeed' = 'true',\n    'quality.layer'              = 'silver'\n)\nAS SELECT\n    txn_id,\n    account_id,\n    CAST(txn_amount AS DECIMAL(18,4))           AS txn_amount,\n    UPPER(TRIM(txn_currency))                   AS txn_currency,\n    CAST(txn_timestamp AS TIMESTAMP)            AS txn_timestamp,\n    DATE(CAST(txn_timestamp AS TIMESTAMP))      AS txn_date,\n    COALESCE(txn_type, 'UNKNOWN')               AS txn_type,\n    COALESCE(txn_status, 'PENDING')             AS txn_status,\n    COALESCE(source_system, 'UNKNOWN')          AS source_system,\n    _ingest_ts                                  AS _bronze_ingest_ts,\n    current_timestamp()                         AS _silver_ts\nFROM STREAM(LIVE.bronze_transactions)\nWHERE _rescued_data IS NULL;  -- Exclude rescued/malformed rows\n\n-- ─── SILVER: FX-Enriched via LIVE table join ──\nCREATE OR REFRESH STREAMING TABLE silver_transactions_enriched\nCOMMENT 'Silver enriched — with EUR normalization'\nAS SELECT\n    t.*,\n    COALESCE(fx.exchange_rate, 1.0)             AS fx_rate,\n    ROUND(t.txn_amount * COALESCE(fx.exchange_rate, 1.0), 4) AS txn_amount_eur\nFROM STREAM(LIVE.silver_transactions) t\nLEFT JOIN LIVE.dim_fx_rates fx   -- Static table (non-streaming JOIN)\n    ON t.txn_currency = fx.from_currency\n    AND t.txn_date    = fx.rate_date\n    AND fx.to_currency = 'EUR';\n\n-- ─── GOLD: Daily Account Summary ──────────────\nCREATE OR REFRESH LIVE TABLE gold_daily_account_summary\nCOMMENT 'Gold — daily account-level aggregation for BI & regulatory reporting'\nTBLPROPERTIES (\n    'delta.autoOptimize.optimizeWrite' = 'true'\n)\nAS SELECT\n    txn_date                            AS snapshot_date,\n    account_id,\n    COUNT(*)                            AS txn_count,\n    SUM(CASE WHEN txn_type='DEBIT'  THEN txn_amount_eur ELSE 0 END) AS total_debit_eur,\n    SUM(CASE WHEN txn_type='CREDIT' THEN txn_amount_eur ELSE 0 END) AS total_credit_eur,\n    SUM(CASE WHEN txn_type='DEBIT'  THEN txn_amount_eur\n             WHEN txn_type='CREDIT' THEN -txn_amount_eur\n             ELSE 0 END)                AS net_flow_eur,\n    MAX(txn_amount_eur)                 AS max_single_txn_eur,\n    COUNT(DISTINCT txn_currency)        AS unique_currencies,\n    SUM(CASE WHEN txn_amount_eur >= 15000 THEN 1 ELSE 0 END) AS high_value_count,\n    current_timestamp()                 AS _gold_ts\nFROM LIVE.silver_transactions_enriched\nGROUP BY txn_date, account_id;",
    "notes": [
      "EXPECT ... ON VIOLATION DROP ROW: removes bad rows; WARN: logs violation but keeps row; FAIL: halts pipeline — choose based on data quality tolerance",
      "STREAM(LIVE.table) reads incrementally from an upstream DLT table — enables multi-hop streaming without duplicates",
      "DLT resolves dependencies automatically from LIVE.* references — no need to manage execution order manually",
      "cloud_files() is the DLT-native Auto Loader function — handles schema inference, evolution, and checkpoint management",
      "DLT expectations are tracked in the Pipeline Events UI — violation rates feed the Data Quality dashboard for SLA monitoring"
    ],
    "use_case": "Production payment processing pipeline at a European fintech — DLT's declarative model with built-in expectations replaces 3,000 lines of imperative PySpark with 200 lines of SQL, while automated retries and monitoring reduce on-call incidents by 70%. The pipeline processes 50M daily transactions with < 5-minute latency from source to gold."
  },
  {
    "id": "arc_003",
    "title": "Unity Catalog Governance: GRANT, REVOKE, Row Filters, Column Masks",
    "level": "architect",
    "category": "Data Governance",
    "description": "Unity Catalog provides fine-grained access control with GRANT/REVOKE at catalog/schema/table/column levels, dynamic row filters for per-user data visibility, and column masks for PII redaction. These replace legacy IP-based ACLs with policy-as-code governance that scales across multi-workspace, multi-regulation BFSI environments.",
    "code": "-- =============================================\n-- Unity Catalog Governance\n-- EU BFSI — GDPR-Compliant Access Control\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\n\n-- ─── 1. GRANT — Catalog/Schema/Table level ────\n\n-- Catalog-level: USE privilege\nGRANT USE CATALOG ON CATALOG eu_bfsi_prod\n    TO `data-analysts`, `compliance-officers`, `bi-team`;\n\n-- Schema-level: USE privilege\nGRANT USE SCHEMA ON SCHEMA eu_bfsi_prod.gold\n    TO `data-analysts`, `bi-team`, `compliance-officers`;\n\n-- Table-level: SELECT only (read-only analysts)\nGRANT SELECT ON TABLE eu_bfsi_prod.gold.gold_daily_account_summary\n    TO `data-analysts`;\n\n-- Silver: Only compliance and engineering can access\nGRANT SELECT ON TABLE eu_bfsi_prod.silver.silver_transactions\n    TO `compliance-officers`, `data-engineers`;\n\n-- Full DML for data engineers\nGRANT SELECT, INSERT, UPDATE, DELETE, MODIFY\n    ON TABLE eu_bfsi_prod.silver.silver_transactions\n    TO `data-engineers`;\n\n-- Schema-wide GRANT for gold read\nGRANT SELECT ON SCHEMA eu_bfsi_prod.gold\n    TO `bi-team`;\n\n-- Function execution\nGRANT EXECUTE ON FUNCTION eu_bfsi_prod.security.mask_iban\n    TO `data-analysts`;\n\n-- ─── 2. REVOKE ─────────────────────────────────\nREVOKE SELECT ON TABLE eu_bfsi_prod.silver.silver_transactions\n    FROM `data-analysts`;\n\n-- ─── 3. ROW FILTER — per-user data visibility ──\n-- Analysts see only their region's data\nCREATE FUNCTION IF NOT EXISTS eu_bfsi_prod.security.filter_by_region (\n    row_country_code STRING\n)\nRETURNS BOOLEAN\nRETURN\n    is_member('data-engineers')         -- Engineers see all\n    OR is_member('compliance-officers') -- Compliance sees all\n    OR (\n        is_member('uk-analysts')  AND row_country_code = 'GB'\n    ) OR (\n        is_member('de-analysts')  AND row_country_code = 'DE'\n    ) OR (\n        is_member('fr-analysts')  AND row_country_code = 'FR'\n    );\n\n-- Apply row filter to table\nALTER TABLE eu_bfsi_prod.gold.gold_daily_account_summary\n    SET ROW FILTER eu_bfsi_prod.security.filter_by_region\n    ON (country_code);\n\n-- ─── 4. COLUMN MASK — PII redaction ────────────\n-- Mask account_id for non-privileged users\nCREATE FUNCTION IF NOT EXISTS eu_bfsi_prod.security.mask_account_id (\n    account_id STRING\n)\nRETURNS STRING\nRETURN CASE\n    WHEN is_member('data-engineers')    THEN account_id\n    WHEN is_member('compliance-officers') THEN\n        CONCAT(LEFT(account_id, 3), '****', RIGHT(account_id, 4))\n    ELSE\n        SHA2(account_id, 256)  -- Pseudonymize for analysts\nEND;\n\nALTER TABLE eu_bfsi_prod.silver.silver_transactions\n    ALTER COLUMN account_id\n    SET MASK eu_bfsi_prod.security.mask_account_id;\n\n-- ─── 5. Show grants / effective permissions ────\nSHOW GRANTS ON TABLE eu_bfsi_prod.silver.silver_transactions;\nSHOW GRANTS ON SCHEMA eu_bfsi_prod.silver;\nSHOW GRANTS ON CATALOG eu_bfsi_prod;\n\n-- ─── 6. Information schema queries ────────────\nSELECT\n    table_catalog, table_schema, table_name,\n    privilege_type, grantee, is_grantable\nFROM eu_bfsi_prod.information_schema.table_privileges\nWHERE table_schema = 'silver'\nORDER BY table_name, grantee;\n\n-- ─── 7. Audit access via system tables ─────────\nSELECT\n    event_time, user_name, action_name,\n    request_params.table_full_name,\n    response.status_code\nFROM system.access.audit\nWHERE event_date >= current_date() - INTERVAL 7 DAYS\n  AND action_name IN ('SELECT', 'INSERT', 'DELETE', 'MERGE')\n  AND request_params.table_full_name\n      LIKE 'eu_bfsi_prod.silver%'\nORDER BY event_time DESC\nLIMIT 500;",
    "notes": [
      "Row filters are transparent — users don't see the filter; they simply get fewer rows with no error message, which is the intended behavior for need-to-know data",
      "Column masks evaluate per-row at query time — use is_member() / is_account_group_member() for group-based logic",
      "GRANT on SCHEMA applies to all current tables but NOT future tables — use GRANT ON FUTURE TABLES for forward-covering grants",
      "system.access.audit provides immutable audit log of all data access events — required for GDPR Article 30 records of processing activities",
      "Unity Catalog GRANTs are workspace-independent — grants made in one workspace apply across all workspaces sharing the metastore"
    ],
    "use_case": "Implementing GDPR Article 25 (data protection by design) at a pan-European insurance company — row filters restrict UK, DE, FR analysts to their jurisdiction's customer data, column masks pseudonymize account IDs for the analytics team, and audit logs feed the quarterly GDPR compliance report automatically from system.access.audit."
  },
  {
    "id": "arc_004",
    "title": "Data Vault 2.0 SQL Patterns: Hub, Link, Satellite",
    "level": "architect",
    "category": "Architecture Patterns",
    "description": "Data Vault 2.0 provides a highly scalable, auditable, and agile data modeling approach for enterprise data warehouses. The three core entity types — Hubs (unique business keys), Links (relationships), and Satellites (context/attributes) — are implemented in Spark SQL with Delta tables and MERGE INTO for parallel, idempotent loads. DV2 is ideal for BFSI environments requiring full auditability.",
    "code": "-- =====================================================================\n-- DATA VAULT 2.0 SQL PATTERNS\n-- EU BFSI — Payment Transaction Data Vault\n-- =====================================================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA dv_raw;  -- Raw Vault schema\n\n-- ─── HUB: Hub_Transaction ─────────────────────────────────────────────\n-- Stores unique business keys for transactions\nCREATE TABLE IF NOT EXISTS hub_transaction (\n    txn_hash_key    STRING  NOT NULL  COMMENT 'MD5(txn_id || source_system)',\n    load_date       TIMESTAMP NOT NULL COMMENT 'First seen timestamp',\n    record_source   STRING  NOT NULL  COMMENT 'System that first provided this key',\n    txn_id          STRING  NOT NULL  COMMENT 'Business key from source'\n)\nUSING DELTA\nCOMMENT 'Hub — unique transaction business keys'\nTBLPROPERTIES ('delta.enableChangeDataFeed'='true');\n\n-- Load Hub (idempotent — only new hash keys inserted)\nMERGE INTO hub_transaction AS tgt\nUSING (\n    SELECT DISTINCT\n        MD5(CONCAT_WS('||', txn_id, source_system))  AS txn_hash_key,\n        MIN(_ingest_ts) OVER (PARTITION BY txn_id)   AS load_date,\n        source_system                                 AS record_source,\n        txn_id\n    FROM eu_bfsi_prod.bronze.bronze_transactions\n    WHERE ingest_date = current_date()\n) AS src\nON tgt.txn_hash_key = src.txn_hash_key\nWHEN NOT MATCHED THEN INSERT *;\n\n-- ─── HUB: Hub_Account ─────────────────────────────────────────────────\nCREATE TABLE IF NOT EXISTS hub_account (\n    acct_hash_key   STRING  NOT NULL,\n    load_date       TIMESTAMP NOT NULL,\n    record_source   STRING  NOT NULL,\n    account_id      STRING  NOT NULL\n)\nUSING DELTA COMMENT 'Hub — unique account business keys';\n\n-- ─── LINK: Link_Transaction_Account ───────────────────────────────────\n-- Represents the relationship: Transaction involves Account\nCREATE TABLE IF NOT EXISTS link_txn_account (\n    link_hash_key   STRING  NOT NULL  COMMENT 'MD5(txn_hash_key || acct_hash_key)',\n    load_date       TIMESTAMP NOT NULL,\n    record_source   STRING  NOT NULL,\n    txn_hash_key    STRING  NOT NULL  COMMENT 'FK → hub_transaction',\n    acct_hash_key   STRING  NOT NULL  COMMENT 'FK → hub_account'\n)\nUSING DELTA COMMENT 'Link — transaction-to-account relationship';\n\nMERGE INTO link_txn_account AS tgt\nUSING (\n    SELECT\n        MD5(CONCAT_WS('||',\n            MD5(CONCAT_WS('||', b.txn_id, b.source_system)),\n            MD5(b.account_id)\n        ))                                          AS link_hash_key,\n        b._ingest_ts                                AS load_date,\n        b.source_system                             AS record_source,\n        MD5(CONCAT_WS('||', b.txn_id, b.source_system)) AS txn_hash_key,\n        MD5(b.account_id)                           AS acct_hash_key\n    FROM eu_bfsi_prod.bronze.bronze_transactions b\n    WHERE ingest_date = current_date()\n) AS src\nON tgt.link_hash_key = src.link_hash_key\nWHEN NOT MATCHED THEN INSERT *;\n\n-- ─── SATELLITE: Sat_Transaction_Details ───────────────────────────────\n-- Stores context/attributes for transactions (versioned)\nCREATE TABLE IF NOT EXISTS sat_transaction_details (\n    txn_hash_key    STRING  NOT NULL  COMMENT 'FK → hub_transaction',\n    load_date       TIMESTAMP NOT NULL COMMENT 'Record effective from',\n    load_end_date   TIMESTAMP         COMMENT 'NULL = current; populated when superseded',\n    record_source   STRING  NOT NULL,\n    hash_diff       STRING  NOT NULL  COMMENT 'MD5 of all satellite attributes for change detection',\n    -- Descriptive attributes\n    txn_amount      DECIMAL(18,4),\n    txn_currency    STRING(3),\n    txn_type        STRING,\n    txn_status      STRING,\n    txn_timestamp   TIMESTAMP,\n    source_system   STRING\n)\nUSING DELTA\nCOMMENT 'Satellite — transaction detail attributes with full history'\nTBLPROPERTIES ('delta.enableChangeDataFeed'='true');\n\n-- Load Satellite (insert-only, no updates)\nINSERT INTO sat_transaction_details\nSELECT\n    MD5(CONCAT_WS('||', b.txn_id, b.source_system))  AS txn_hash_key,\n    b._ingest_ts                                       AS load_date,\n    NULL                                               AS load_end_date,\n    b.source_system                                    AS record_source,\n    MD5(CONCAT_WS('||',\n        COALESCE(CAST(b.txn_raw_amount AS STRING), ''),\n        COALESCE(b.txn_currency, ''),\n        COALESCE(b.txn_type, ''),\n        COALESCE(b.txn_status, ''),\n        COALESCE(CAST(b.txn_timestamp_raw AS STRING), '')\n    ))                                                 AS hash_diff,\n    CAST(REGEXP_REPLACE(b.txn_raw_amount, '[^0-9.]', '') AS DECIMAL(18,4)) AS txn_amount,\n    UPPER(TRIM(b.txn_currency))                        AS txn_currency,\n    b.txn_type,\n    b.txn_status,\n    CAST(b.txn_timestamp_raw AS TIMESTAMP)             AS txn_timestamp,\n    b.source_system\nFROM eu_bfsi_prod.bronze.bronze_transactions b\nWHERE NOT EXISTS (\n    -- Only insert if hash_diff changed (new attributes)\n    SELECT 1 FROM sat_transaction_details s\n    WHERE s.txn_hash_key = MD5(CONCAT_WS('||', b.txn_id, b.source_system))\n      AND s.hash_diff = MD5(CONCAT_WS('||',\n            COALESCE(CAST(b.txn_raw_amount AS STRING), ''),\n            COALESCE(b.txn_currency, ''),\n            COALESCE(b.txn_type, ''),\n            COALESCE(b.txn_status, ''),\n            COALESCE(CAST(b.txn_timestamp_raw AS STRING), '')\n        ))\n      AND s.load_end_date IS NULL\n);",
    "notes": [
      "Hub stores ONLY the business key and hash key — all attributes belong in satellites; this enforces separation of concerns",
      "Hash keys (MD5/SHA1 of business key + source) enable parallel loading of all vault entities without sequence generators",
      "Satellites are insert-only append tables — hash_diff change detection avoids re-inserting unchanged records",
      "Links require all related hub hash keys — they store relationships with timestamps, enabling full temporal analysis of relationship history",
      "DV2's parallel load pattern (Hub/Link/Sat load simultaneously) scales linearly — a 1-hour sequential load becomes 15 minutes with parallel Databricks jobs"
    ],
    "use_case": "Building a Data Vault 2.0 raw vault for a pan-European bank's enterprise data warehouse — Hub/Link/Sat pattern absorbs schema changes from Core Banking, SWIFT, and PSD2 Open Banking feeds without breaking downstream consumers, while the hash-key approach enables parallel loading of 200M daily records across 50 Databricks worker nodes."
  },
  {
    "id": "arc_005",
    "title": "GDPR Erasure with DELETE and VACUUM",
    "level": "architect",
    "category": "Data Governance",
    "description": "GDPR Article 17 (Right to Erasure) requires physically removing personal data across all layers of the data lake within 30 days of a verified erasure request. Delta's DELETE combined with column mapping and VACUUM provides a compliant erasure workflow. The challenge is cascading deletions across Bronze, Silver, and Gold layers while maintaining referential integrity.",
    "code": "-- =============================================\n-- GDPR Right to Erasure — Full Pipeline\n-- EU BFSI — Customer Data Deletion Workflow\n-- Author: Santosh Jammi, TCS Data Engineering\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\n\n-- ─── STEP 0: Create erasure request tracking table ────\nCREATE TABLE IF NOT EXISTS eu_bfsi_prod.governance.gdpr_erasure_requests (\n    request_id          STRING NOT NULL,\n    customer_id         STRING NOT NULL,\n    requested_by        STRING,\n    request_date        DATE NOT NULL,\n    approved_date       DATE,\n    approval_status     STRING COMMENT 'PENDING | APPROVED | REJECTED | COMPLETED',\n    erasure_scope       ARRAY<STRING> COMMENT 'List of tables to erase from',\n    completed_ts        TIMESTAMP,\n    completion_notes    STRING\n) USING DELTA;\n\n-- ─── STEP 1: Approve and scope the erasure request ────\nINSERT INTO eu_bfsi_prod.governance.gdpr_erasure_requests\nVALUES (\n    'GDPR-2024-001247',\n    'CUST-EU-83847291',\n    'dpo@eurobank.eu',\n    '2024-12-15',\n    '2024-12-16',\n    'APPROVED',\n    ARRAY(\n        'silver.silver_customers',\n        'silver.silver_accounts',\n        'silver.silver_transactions',\n        'bronze.bronze_transactions',\n        'gold.gold_daily_account_summary'\n    ),\n    NULL,\n    NULL\n);\n\n-- ─── STEP 2: Collect all affected identifiers ─────────\nCREATE OR REPLACE TEMPORARY VIEW v_erasure_scope AS\nSELECT\n    r.customer_id,\n    a.account_id,\n    r.request_id\nFROM eu_bfsi_prod.governance.gdpr_erasure_requests r\nJOIN eu_bfsi_prod.silver.silver_accounts a\n    ON r.customer_id = a.customer_id\nWHERE r.request_id = 'GDPR-2024-001247'\n  AND r.approval_status = 'APPROVED';\n\n-- ─── STEP 3: Delete PII from Silver customers ────────\nDELETE FROM eu_bfsi_prod.silver.silver_customers\nWHERE customer_id IN (\n    SELECT customer_id FROM v_erasure_scope\n);\n\n-- ─── STEP 4: Pseudonymize transaction references ──────\n-- (Cannot delete transactions — needed for financial audit)\n-- Replace PII fields with pseudonymous tokens\nUPDATE eu_bfsi_prod.silver.silver_transactions\nSET\n    account_id      = SHA2(CONCAT(account_id, 'GDPR_SALT_2024'), 256),\n    counterparty_id = SHA2(CONCAT(counterparty_id, 'GDPR_SALT_2024'), 256)\nWHERE account_id IN (\n    SELECT account_id FROM v_erasure_scope\n);\n\n-- ─── STEP 5: Delete from bronze (raw data) ───────────\nDELETE FROM eu_bfsi_prod.bronze.bronze_transactions\nWHERE account_id IN (\n    SELECT account_id FROM v_erasure_scope\n);\n\n-- ─── STEP 6: Delete from gold aggregations ───────────\nDELETE FROM eu_bfsi_prod.gold.gold_daily_account_summary\nWHERE account_id IN (\n    SELECT account_id FROM v_erasure_scope\n);\n\n-- ─── STEP 7: Physical file deletion with VACUUM ───────\n-- CRITICAL: Set retention to 0 ONLY for GDPR compliance\n-- First, disable safety check:\nSET spark.databricks.delta.retentionDurationCheck.enabled = false;\n\nVACUUM eu_bfsi_prod.silver.silver_customers RETAIN 0 HOURS;\nVACUUM eu_bfsi_prod.bronze.bronze_transactions RETAIN 0 HOURS;\nVACUUM eu_bfsi_prod.silver.silver_transactions RETAIN 0 HOURS;\nVACUUM eu_bfsi_prod.gold.gold_daily_account_summary RETAIN 0 HOURS;\n\n-- Re-enable safety check\nSET spark.databricks.delta.retentionDurationCheck.enabled = true;\n\n-- ─── STEP 8: Mark request as completed ───────────────\nUPDATE eu_bfsi_prod.governance.gdpr_erasure_requests\nSET\n    approval_status = 'COMPLETED',\n    completed_ts    = current_timestamp(),\n    completion_notes = 'PII deleted from 4 tables; transactions pseudonymized; VACUUM completed'\nWHERE request_id = 'GDPR-2024-001247';\n\n-- ─── STEP 9: Verify erasure ───────────────────────────\nSELECT COUNT(*) AS remaining_records\nFROM eu_bfsi_prod.silver.silver_customers\nWHERE customer_id = 'CUST-EU-83847291';\n-- Expected: 0",
    "notes": [
      "VACUUM RETAIN 0 HOURS physically removes Parquet files from storage — do this ONLY for GDPR; normal operations should keep 7+ days",
      "Disable retentionDurationCheck only temporarily and in the scope of GDPR deletion — never as a permanent setting",
      "Financial transactions often cannot be deleted (money laundering regulations require 5-7 year retention) — pseudonymization with SHA256 is the compliant alternative",
      "Time travel to versions before deletion becomes impossible after VACUUM — this is intentional for GDPR compliance",
      "Maintain the erasure_requests audit table permanently — it's your proof of compliance without retaining the erased PII"
    ],
    "use_case": "GDPR Article 17 erasure automation at a European digital bank — a former customer exercises their right to erasure; the workflow deletes PII from Silver/Bronze/Gold layers, pseudonymizes transaction references (preserving financial audit trail), runs VACUUM to purge old file versions, and generates a compliance certificate within 72 hours of the request."
  },
  {
    "id": "arc_006",
    "title": "FinOps Table Partitioning and Cost Optimization Strategies",
    "level": "architect",
    "category": "FinOps & Performance",
    "description": "FinOps in Azure Databricks requires minimizing DBU consumption and ADLS storage costs while maintaining query performance SLAs. Strategic partitioning, Liquid Clustering, Photon engine optimization, and compute auto-scaling are the primary levers. Poor partitioning (too many small files or over-partitioned tables) is the #1 cause of runaway Databricks costs in BFSI platforms.",
    "code": "-- =============================================\n-- FinOps Partitioning & Cost Optimization\n-- EU BFSI — Azure Databricks Cost Engineering\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA silver;\n\n-- ─── 1. Diagnose partition health ─────────────\n-- Check number of files and sizes per partition\nSELECT\n    partition.txn_date,\n    COUNT(*)                AS num_files,\n    SUM(size) / 1e9         AS total_size_gb,\n    AVG(size) / 1e6         AS avg_file_size_mb,\n    MIN(size) / 1e3         AS min_file_kb,\n    MAX(size) / 1e6         AS max_file_mb,\n    SUM(CASE WHEN size < 1048576 THEN 1 ELSE 0 END) AS tiny_files_under_1mb\nFROM (\n    DESCRIBE DETAIL eu_bfsi_prod.silver.silver_transactions\n)\nCROSS JOIN (\n    SHOW PARTITIONS eu_bfsi_prod.silver.silver_transactions\n) partition\nGROUP BY partition.txn_date\nORDER BY tiny_files_under_1mb DESC\nLIMIT 30;\n\n-- ─── 2. Optimal partitioning strategies ───────\n\n-- Strategy A: Date partitioning (most common for time-series BFSI data)\n-- Good for: daily batch queries, time-range scans\n-- Target: 128MB - 1GB per partition file\nCREATE TABLE IF NOT EXISTS silver_transactions_v2\nUSING DELTA\nPARTITIONED BY (txn_date)  -- ~1 partition per day, sized for daily volume\nCOMMENT 'Date-partitioned silver transactions';\n\n-- Strategy B: Liquid Clustering (DBR 13.3+ — preferred over static partitioning)\n-- Allows incremental re-clustering without full rewrites\nCREATE TABLE IF NOT EXISTS silver_transactions_lc\nUSING DELTA\nCLUSTER BY (txn_date, account_id, txn_currency)\nCOMMENT 'Liquid-clustered silver transactions — auto-optimized';\n\n-- ─── 3. Auto-optimize settings for cost control ─\nALTER TABLE silver_transactions\nSET TBLPROPERTIES (\n    'delta.autoOptimize.optimizeWrite'   = 'true',  -- Bin-pack on write\n    'delta.autoOptimize.autoCompact'     = 'true',  -- Compact in background\n    'delta.targetFileSize'               = '134217728',  -- 128MB target\n    'delta.tuneFileSizesForRewrites'     = 'true'   -- Optimize for MERGE\n);\n\n-- ─── 4. Statistics collection for better plans ──\nANALYZE TABLE silver_transactions\nCOMPUTE STATISTICS FOR ALL COLUMNS;\n\n-- ─── 5. Partition pruning verification ──────────\n-- EXPLAIN shows partition skipping\nEXPLAIN COST\nSELECT COUNT(*)\nFROM silver_transactions\nWHERE txn_date = '2024-12-31';  -- Should show: PartitionFilters: [txn_date = 2024-12-31]\n\n-- ─── 6. Archive cold partitions to cheaper storage\n-- Move partitions older than 2 years to archive tier\nCREATE TABLE IF NOT EXISTS eu_bfsi_prod.archive.silver_transactions_archive\nUSING DELTA\nLOCATION 'abfss://archive@euadlsarchive.dfs.core.windows.net/transactions/'\nCOMMENT 'Archive — transactions > 2 years (Cool tier ADLS)';\n\n-- Insert old data into archive then delete from hot\nINSERT INTO eu_bfsi_prod.archive.silver_transactions_archive\nSELECT * FROM eu_bfsi_prod.silver.silver_transactions\nWHERE txn_date < DATE_SUB(current_date(), 730);\n\n-- Then delete from hot tier (after verifying archive)\nDELETE FROM eu_bfsi_prod.silver.silver_transactions\nWHERE txn_date < DATE_SUB(current_date(), 730);\nVACUUM eu_bfsi_prod.silver.silver_transactions RETAIN 168 HOURS;\n\n-- ─── 7. Query optimization via SET configs ──────\n-- Reduce shuffle partitions for small datasets (cost saving)\nSET spark.sql.shuffle.partitions = 200;  -- Default 200; reduce for < 10GB queries\n\n-- Enable AQE (default in Spark 3.x)\nSET spark.sql.adaptive.enabled = true;\nSET spark.sql.adaptive.coalescePartitions.enabled = true;\nSET spark.sql.adaptive.skewJoin.enabled = true;\n\n-- ─── 8. Bloom filter for high-cardinality lookups\nCREATE BLOOMFILTER INDEX ON TABLE silver_transactions\nFOR COLUMNS (\n    txn_id OPTIONS (fpp=0.001, numItems=5000000000),\n    account_id OPTIONS (fpp=0.01, numItems=10000000)\n);",
    "notes": [
      "Target 128MB-1GB file sizes in Delta — tiny files (<1MB) cause excessive file listing overhead and slow queries significantly",
      "Liquid Clustering outperforms static partitioning for high-cardinality dimensions — it clusters on multiple columns without exploding partition count",
      "ADLS Gen2 tiering: Hot (frequently accessed), Cool (infrequently accessed, cheaper), Archive (rare access, much cheaper) — move >2-year data to Cool tier for 50% storage cost saving",
      "Adaptive Query Execution (AQE) dynamically coalesces shuffle partitions, handles skew joins, and switches join strategies at runtime — keep enabled for all production clusters",
      "Bloom filters dramatically speed up equality lookups on high-cardinality columns (txn_id, account_id) — each file's bloom filter prevents unnecessary file reads for point lookups"
    ],
    "use_case": "FinOps optimization at a European digital bank where uncontrolled Databricks spend had grown 340% in 18 months — audit reveals 2.3 billion tiny files from 3 years of unoptimized streaming writes. Liquid Clustering + OPTIMIZE + VACUUM + ADLS tiering reduces monthly Azure spend by 58% while improving P95 query latency from 4 minutes to 35 seconds."
  },
  {
    "id": "arc_007",
    "title": "Microsoft Fabric Lakehouse SQL Endpoint and Cross-Workspace Queries",
    "level": "architect",
    "category": "Microsoft Fabric",
    "description": "Microsoft Fabric's Lakehouse exposes a SQL Analytics Endpoint (formerly SQL Server endpoint) that supports T-SQL queries directly against Delta tables in OneLake. The unified OneLake storage model enables cross-workspace queries and shortcut-based data sharing without data movement. Fabric's Direct Lake mode powers Power BI with columnar in-memory performance over Delta files.",
    "code": "-- =============================================\n-- Microsoft Fabric Lakehouse SQL\n-- EU BFSI — Cross-Workspace Analytics\n-- =============================================\n\n-- ─── 1. Fabric SQL Analytics Endpoint (T-SQL compatible) ─\n-- Access via: SQL connection string in Fabric workspace\n-- jdbc:sqlserver://<workspace>.datawarehouse.fabric.microsoft.com\n\n-- Query Delta table via SQL Analytics Endpoint\nSELECT TOP 100\n    txn_id,\n    account_id,\n    txn_amount,\n    txn_currency,\n    txn_date\nFROM [eu_bfsi_lakehouse].[dbo].[silver_transactions]\nWHERE txn_date = CAST(GETDATE() AS DATE)\n  AND txn_amount > 10000\nORDER BY txn_amount DESC;\n\n-- ─── 2. OneLake Shortcut — cross-workspace data sharing\n-- (Configured via Fabric portal; queries are transparent)\n-- Shortcut from workspace B to workspace A's Delta table:\nSELECT COUNT(*), SUM(txn_amount_eur)\nFROM [eu_analytics_lakehouse].[dbo].[silver_transactions]  -- Shortcut to prod lakehouse\nWHERE txn_date >= DATEADD(DAY, -30, GETDATE());\n\n-- ─── 3. Fabric Warehouse — cross-item queries ─────────────\n-- Query across Lakehouse and Warehouse in same workspace\nSELECT\n    lh.account_id,\n    lh.txn_amount_eur,\n    wh.customer_segment,\n    wh.relationship_manager\nFROM [eu_bfsi_lakehouse].[dbo].[silver_transactions] lh  -- Lakehouse (Delta)\nJOIN [eu_bfsi_warehouse].[dbo].[dim_customers] wh         -- Fabric Warehouse table\n    ON lh.account_id = wh.account_id\nWHERE lh.txn_date = CAST(GETDATE()-1 AS DATE);\n\n-- ─── 4. Azure Databricks ↔ Fabric interop ────────────────\n-- Option A: Fabric shortcut to ADLS Gen2 (where Databricks Delta tables live)\n-- Create shortcut in Fabric to:\n-- abfss://silver@euadlsprod.dfs.core.windows.net/silver_transactions/\n-- → Available as: [eu_bfsi_lakehouse].[dbo].[silver_transactions]\n\n-- Option B: From Databricks, write to Fabric OneLake via ABFSS connector\n-- Databricks SQL writing to Fabric OneLake:\nCREATE TABLE IF NOT EXISTS fabric.eu_bfsi_lakehouse.silver_transactions_export\nUSING DELTA\nLOCATION 'abfss://eu_bfsi_lakehouse@onelake.dfs.fabric.microsoft.com/silver_transactions/'\nAS SELECT * FROM eu_bfsi_prod.silver.silver_transactions\nWHERE txn_date = current_date() - INTERVAL 1 DAY;\n\n-- ─── 5. Direct Lake — Fabric Power BI optimization ────────\n-- Direct Lake reads Delta Parquet files directly into memory\n-- No import, no DirectQuery latency — native columnar performance\n-- Trigger Direct Lake framing refresh via SQL:\n-- EXEC sp_refreshsqlmodule '[dbo].[silver_transactions]';\n\n-- ─── 6. Fabric SQL — GDPR-compliant view ─────────────────\nCREATE OR ALTER VIEW [dbo].[v_silver_transactions_masked]\nAS\nSELECT\n    txn_id,\n    -- Mask account_id using SQL dynamic data masking\n    CASE\n        WHEN IS_MEMBER('DataEngineers') = 1 THEN account_id\n        ELSE CONCAT('****', RIGHT(account_id, 6))\n    END AS account_id,\n    txn_amount,\n    txn_currency,\n    txn_date,\n    txn_type\nFROM [dbo].[silver_transactions];\n\n-- Grant access to view (not underlying table)\nGRANT SELECT ON [dbo].[v_silver_transactions_masked]\n    TO [eu-analysts-sg@bank.eu];",
    "notes": [
      "Fabric SQL Analytics Endpoint is T-SQL compatible — existing SQL Server queries work with minimal changes, lowering migration effort",
      "OneLake Shortcuts eliminate data duplication — the same Delta file is queryable from Databricks, Fabric Lakehouse, and Fabric Warehouse simultaneously",
      "Direct Lake mode achieves Power BI report render times under 2 seconds on billion-row Delta tables — it frames data into columnar memory cache on demand",
      "ABFSS connector allows Databricks to write directly to Fabric OneLake — enabling hybrid architectures where Databricks processes and Fabric serves",
      "Cross-workspace queries in Fabric use the workspace identity for authorization — ensure OneLake permissions are configured at both workspace and item levels"
    ],
    "use_case": "A European universal bank's analytics modernization — Azure Databricks runs the heavy ETL (Bronze→Silver→Gold via DLT), writes Delta tables to ADLS Gen2, and Fabric shortcuts expose the same tables to Power BI via Direct Lake, eliminating the legacy SQL Server data warehouse while reducing time-to-insight from daily batch to real-time."
  },
  {
    "id": "arc_008",
    "title": "Databricks SQL Functions and Procedures (CREATE FUNCTION, SQL UDF)",
    "level": "architect",
    "category": "Reusable SQL Components",
    "description": "Databricks supports SQL User Defined Functions (UDFs), Python UDFs registered in Unity Catalog, and stored procedures-like patterns using EXECUTE IMMEDIATE. SQL UDFs enable reusable, versionable business logic — critical for regulatory calculations (PD, LGD, risk scoring) that must be consistent across all pipelines and consumers.",
    "code": "-- =============================================\n-- SQL Functions and Reusable Logic\n-- EU BFSI — Regulatory Calculation Library\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\n\n-- ─── 1. Scalar SQL UDF ────────────────────────\nCREATE OR REPLACE FUNCTION eu_bfsi_prod.utils.calculate_ead (\n    outstanding_balance DECIMAL(18, 4),\n    undrawn_commitment  DECIMAL(18, 4),\n    credit_conversion_factor DECIMAL(5, 4)  -- CCF: 0.0 to 1.0\n)\nRETURNS DECIMAL(18, 4)\nCOMMENT 'Exposure at Default (EAD) = Outstanding + CCF * Undrawn. Used for COREP credit risk.'\nRETURN outstanding_balance + (credit_conversion_factor * undrawn_commitment);\n\n-- Usage:\nSELECT\n    account_id,\n    eu_bfsi_prod.utils.calculate_ead(\n        outstanding_balance,\n        undrawn_commitment,\n        0.75  -- 75% CCF for revolving facilities\n    ) AS ead_exposure\nFROM eu_bfsi_prod.silver.silver_credit_facilities;\n\n-- ─── 2. Table-valued SQL UDF ──────────────────\nCREATE OR REPLACE FUNCTION eu_bfsi_prod.utils.get_account_transactions (\n    p_account_id STRING,\n    p_from_date  DATE,\n    p_to_date    DATE\n)\nRETURNS TABLE (\n    txn_id          STRING,\n    txn_amount      DECIMAL(18,4),\n    txn_currency    STRING,\n    txn_date        DATE,\n    txn_type        STRING\n)\nCOMMENT 'Returns transactions for an account in a date range'\nRETURN\nSELECT txn_id, txn_amount, txn_currency, txn_date, txn_type\nFROM eu_bfsi_prod.silver.silver_transactions\nWHERE account_id = p_account_id\n  AND txn_date BETWEEN p_from_date AND p_to_date;\n\n-- Usage:\nSELECT * FROM eu_bfsi_prod.utils.get_account_transactions(\n    'ACC-EU-12345678', '2024-01-01', '2024-12-31'\n);\n\n-- ─── 3. Risk scoring UDF with CASE logic ──────\nCREATE OR REPLACE FUNCTION eu_bfsi_prod.utils.calculate_risk_score (\n    days_past_due       INT,\n    debt_to_income      DECIMAL(5,4),\n    credit_utilization  DECIMAL(5,4),\n    num_recent_inquiries INT\n)\nRETURNS INT\nCOMMENT 'Simplified credit risk score 0-100. Higher = higher risk.'\nRETURN\n    -- Days past due component (40 pts max)\n    CASE\n        WHEN days_past_due = 0          THEN 0\n        WHEN days_past_due <= 30        THEN 15\n        WHEN days_past_due <= 60        THEN 25\n        WHEN days_past_due <= 90        THEN 35\n        ELSE                                 40\n    END\n    -- DTI component (30 pts max)\n    + CASE\n        WHEN debt_to_income < 0.35      THEN 0\n        WHEN debt_to_income < 0.50      THEN 10\n        WHEN debt_to_income < 0.65      THEN 20\n        ELSE                                 30\n    END\n    -- Utilization component (20 pts max)\n    + CASE\n        WHEN credit_utilization < 0.30  THEN 0\n        WHEN credit_utilization < 0.60  THEN 7\n        WHEN credit_utilization < 0.80  THEN 14\n        ELSE                                 20\n    END\n    -- Inquiries component (10 pts max)\n    + LEAST(num_recent_inquiries * 2, 10);\n\n-- ─── 4. EXECUTE IMMEDIATE — dynamic SQL ───────\n-- Stored procedure-like pattern in Databricks\nDECLARE table_name STRING DEFAULT 'silver_transactions';\nDECLARE schema_name STRING DEFAULT 'silver';\nDECLARE row_count BIGINT;\n\nEXECUTE IMMEDIATE\n    CONCAT('SELECT COUNT(*) FROM eu_bfsi_prod.', schema_name, '.', table_name,\n           ' WHERE txn_date = current_date()')\n    INTO row_count;\n\nSELECT row_count AS todays_record_count;\n\n-- ─── 5. Grant function access ─────────────────\nGRANT EXECUTE ON FUNCTION eu_bfsi_prod.utils.calculate_ead\n    TO `risk-analysts`, `compliance-officers`, `data-engineers`;\n\nGRANT EXECUTE ON FUNCTION eu_bfsi_prod.utils.get_account_transactions\n    TO `data-analysts`;\n\n-- ─── 6. List all functions in schema ──────────\nSHOW FUNCTIONS IN eu_bfsi_prod.utils;",
    "notes": [
      "SQL UDFs in Unity Catalog are first-class securables — GRANT EXECUTE controls who can call them, enabling function-level access control",
      "Table-valued functions (RETURNS TABLE) are composable — use them in FROM clause like a table, enabling modular pipeline building",
      "SQL UDFs are inlined into the query plan when possible — no serialization overhead unlike Python UDFs which require inter-process communication",
      "EXECUTE IMMEDIATE enables parameterized dynamic SQL — use for metadata-driven pipeline frameworks where table/column names vary",
      "Register business-critical formulas (EAD, LGD, PD) as versioned SQL UDFs — ensures calculation consistency across 50+ pipelines accessing the same regulatory logic"
    ],
    "use_case": "Regulatory calculation library at a European bank's risk platform — SQL UDFs encapsulate EAD, LGD, and credit scoring formulas reviewed and approved by the Model Risk Management team. All 23 pipelines (credit risk, stress testing, COREP) call the same UDF, ensuring a single change to the CCF parameter updates all downstream calculations simultaneously."
  },
  {
    "id": "arc_009",
    "title": "Data Quality Checks in SQL (Expectations, DQ Framework)",
    "level": "architect",
    "category": "Data Quality",
    "description": "Enterprise-grade data quality requires declarative rules, automated monitoring, and integration with pipeline execution. In Databricks, DQ is implemented via DLT expectations (declarative), SQL-based assertion tables, and Great Expectations-style check patterns. Financial regulatory data must meet SLA-bound quality thresholds — failed DQ can trigger pipeline halts and alert the data engineering team.",
    "code": "-- =============================================\n-- SQL Data Quality Framework\n-- EU BFSI — Payment Data Quality Checks\n-- =============================================\n\nUSE CATALOG eu_bfsi_prod;\nUSE SCHEMA governance;\n\n-- ─── 1. DQ Rules Catalog Table ────────────────\nCREATE TABLE IF NOT EXISTS dq_rules (\n    rule_id         STRING NOT NULL,\n    table_fqn       STRING NOT NULL COMMENT 'catalog.schema.table',\n    rule_name       STRING NOT NULL,\n    rule_type       STRING COMMENT 'NOT_NULL | RANGE | FORMAT | REFERENTIAL | CUSTOM_SQL',\n    column_name     STRING,\n    rule_sql        STRING NOT NULL COMMENT 'SQL expression that returns TRUE for VALID rows',\n    severity        STRING COMMENT 'CRITICAL | HIGH | MEDIUM | LOW',\n    threshold_pct   DECIMAL(5,2) COMMENT 'Acceptable fail % (e.g. 0.0 = zero tolerance)',\n    owner           STRING,\n    active          BOOLEAN DEFAULT TRUE\n) USING DELTA;\n\n-- Load rules\nINSERT INTO dq_rules VALUES\n('DQ-001', 'eu_bfsi_prod.silver.silver_transactions', 'txn_id_not_null',\n 'NOT_NULL', 'txn_id', 'txn_id IS NOT NULL', 'CRITICAL', 0.0, 'data-eng@bank.eu', true),\n('DQ-002', 'eu_bfsi_prod.silver.silver_transactions', 'positive_amount',\n 'RANGE', 'txn_amount', 'txn_amount > 0', 'CRITICAL', 0.0, 'data-eng@bank.eu', true),\n('DQ-003', 'eu_bfsi_prod.silver.silver_transactions', 'valid_currency',\n 'FORMAT', 'txn_currency', \"txn_currency RLIKE '^[A-Z]{3}$'\", 'HIGH', 0.01, 'data-eng@bank.eu', true),\n('DQ-004', 'eu_bfsi_prod.silver.silver_transactions', 'future_timestamp',\n 'RANGE', 'txn_timestamp', 'txn_timestamp <= current_timestamp()', 'HIGH', 0.0, 'data-eng@bank.eu', true),\n('DQ-005', 'eu_bfsi_prod.silver.silver_transactions', 'valid_txn_type',\n 'FORMAT', 'txn_type', \"txn_type IN ('DEBIT','CREDIT','FEE','REVERSAL','INTERNAL')\", 'MEDIUM', 0.1, 'data-eng@bank.eu', true);\n\n-- ─── 2. DQ Results Table ──────────────────────\nCREATE TABLE IF NOT EXISTS dq_results (\n    run_id          STRING NOT NULL,\n    run_ts          TIMESTAMP NOT NULL,\n    rule_id         STRING NOT NULL,\n    table_fqn       STRING NOT NULL,\n    total_rows      BIGINT,\n    passing_rows    BIGINT,\n    failing_rows    BIGINT,\n    pass_rate_pct   DECIMAL(5,2),\n    severity        STRING,\n    threshold_pct   DECIMAL(5,2),\n    dq_status       STRING COMMENT 'PASS | FAIL | WARN',\n    sample_failures STRING COMMENT 'JSON sample of failed rows'\n) USING DELTA;\n\n-- ─── 3. DQ Check Execution (run per-table) ────\nINSERT INTO dq_results\nWITH base AS (\n    SELECT\n        '${run_id}'                             AS run_id,\n        current_timestamp()                     AS run_ts,\n        r.rule_id,\n        r.table_fqn,\n        r.severity,\n        r.threshold_pct\n    FROM dq_rules r\n    WHERE r.table_fqn = 'eu_bfsi_prod.silver.silver_transactions'\n      AND r.active = true\n),\ncheck_results AS (\n    SELECT\n        'DQ-001' AS rule_id,\n        COUNT(*) AS total_rows,\n        SUM(CASE WHEN txn_id IS NOT NULL THEN 1 ELSE 0 END) AS passing_rows,\n        SUM(CASE WHEN txn_id IS NULL THEN 1 ELSE 0 END) AS failing_rows\n    FROM eu_bfsi_prod.silver.silver_transactions\n    WHERE txn_date = current_date()\n    UNION ALL\n    SELECT 'DQ-002', COUNT(*),\n        SUM(CASE WHEN txn_amount > 0 THEN 1 ELSE 0 END),\n        SUM(CASE WHEN txn_amount <= 0 THEN 1 ELSE 0 END)\n    FROM eu_bfsi_prod.silver.silver_transactions WHERE txn_date = current_date()\n    UNION ALL\n    SELECT 'DQ-003', COUNT(*),\n        SUM(CASE WHEN txn_currency RLIKE '^[A-Z]{3}$' THEN 1 ELSE 0 END),\n        SUM(CASE WHEN NOT (txn_currency RLIKE '^[A-Z]{3}$') THEN 1 ELSE 0 END)\n    FROM eu_bfsi_prod.silver.silver_transactions WHERE txn_date = current_date()\n)\nSELECT\n    b.run_id,\n    b.run_ts,\n    b.rule_id,\n    b.table_fqn,\n    cr.total_rows,\n    cr.passing_rows,\n    cr.failing_rows,\n    ROUND(100.0 * cr.passing_rows / cr.total_rows, 4) AS pass_rate_pct,\n    b.severity,\n    b.threshold_pct,\n    CASE\n        WHEN 100.0 * cr.failing_rows / cr.total_rows <= b.threshold_pct THEN 'PASS'\n        WHEN b.severity = 'CRITICAL' THEN 'FAIL'\n        ELSE 'WARN'\n    END AS dq_status,\n    NULL AS sample_failures\nFROM base b\nJOIN check_results cr ON b.rule_id = cr.rule_id;\n\n-- ─── 4. DQ Dashboard Query ────────────────────\nSELECT\n    run_ts,\n    rule_id,\n    table_fqn,\n    total_rows,\n    failing_rows,\n    pass_rate_pct,\n    severity,\n    dq_status\nFROM dq_results\nWHERE DATE(run_ts) = current_date()\nORDER BY dq_status DESC, severity, pass_rate_pct;",
    "notes": [
      "DQ rules as data (not code) enables non-engineers to configure quality checks via a UI or spreadsheet import — accelerates governance adoption",
      "DLT EXPECT clauses are the most elegant option for streaming pipelines; SQL assertion tables work better for batch-oriented governance frameworks",
      "Threshold-based pass/fail (e.g., 0.01% fail rate allowed for optional fields) prevents false alarms from genuinely nullable columns",
      "Store sample failing rows as JSON in dq_results — enables root-cause analysis without re-running the check",
      "Integrate DQ results with Databricks Asset Bundles monitoring and PagerDuty/Teams alerts to close the incident response loop"
    ],
    "use_case": "Regulatory data quality gate at a European bank's FINREP reporting pipeline — 47 SQL-based DQ rules run after each silver layer load; CRITICAL rule failures halt the gold layer job and alert the data engineering on-call via Teams webhook, preventing out-of-tolerance data from reaching the regulatory submission portal."
  },
  {
    "id": "arc_010",
    "title": "APPLY CHANGES INTO — DLT CDC Pattern",
    "level": "architect",
    "category": "Delta Live Tables",
    "description": "APPLY CHANGES INTO is a DLT-exclusive SQL command that implements CDC (Change Data Capture) processing. It consumes ordered change events from a source (insert/update/delete) and applies them to a target table with automatic ordering, deduplication, and SCD Type 1 or Type 2 handling. This is the most sophisticated streaming CDC pattern available in Databricks.",
    "code": "-- =============================================\n-- APPLY CHANGES INTO — DLT CDC Pipeline\n-- EU BFSI — Core Banking Account CDC\n-- =============================================\n-- File: /pipeline/cdc_account_pipeline.sql\n-- Pipeline mode: Enhanced Autoscaling, Triggered\n-- =============================================\n\n-- ─── SOURCE: Streaming CDC events from Debezium/Kafka ─\nCREATE OR REFRESH STREAMING TABLE bronze_account_cdc\nCOMMENT 'Bronze — raw CDC events from Core Banking Debezium connector'\nAS SELECT\n    CAST(key AS STRING)                     AS account_id,\n    get_json_object(CAST(value AS STRING), '$.op')          AS operation,\n    get_json_object(CAST(value AS STRING), '$.ts_ms')       AS event_ts_ms,\n    get_json_object(CAST(value AS STRING), '$.after.account_id')       AS after_account_id,\n    get_json_object(CAST(value AS STRING), '$.after.account_type')     AS after_account_type,\n    get_json_object(CAST(value AS STRING), '$.after.risk_tier')        AS after_risk_tier,\n    get_json_object(CAST(value AS STRING), '$.after.kyc_status')       AS after_kyc_status,\n    get_json_object(CAST(value AS STRING), '$.after.current_balance')  AS after_current_balance,\n    get_json_object(CAST(value AS STRING), '$.after.account_status')   AS after_account_status,\n    _metadata.timestamp                     AS kafka_ingested_ts\nFROM cloud_files(\n    'abfss://kafka-dump@euadlsprod.dfs.core.windows.net/core-banking-cdc/',\n    'json'\n);\n\n-- ─── APPLY CHANGES INTO — SCD Type 1 (latest wins) ───\nAPPLY CHANGES INTO LIVE.silver_accounts_scd1\nFROM STREAM(LIVE.bronze_account_cdc)\nKEYS (account_id)\nAPPLY AS DELETE WHEN operation = 'd'\nAPPLY AS TRUNCATE WHEN operation = 't'\nSEQUENCE BY CAST(event_ts_ms AS BIGINT)\nCOLUMNS * EXCEPT (operation, event_ts_ms, kafka_ingested_ts)\nSTORED AS SCD TYPE 1;\n-- Result: One row per account — always reflects latest state\n\n-- ─── APPLY CHANGES INTO — SCD Type 2 (full history) ──\nAPPLY CHANGES INTO LIVE.silver_accounts_scd2\nFROM STREAM(LIVE.bronze_account_cdc)\nKEYS (account_id)\nAPPLY AS DELETE WHEN operation = 'd'\nSEQUENCE BY CAST(event_ts_ms AS BIGINT)\nCOLUMNS after_account_id, after_account_type, after_risk_tier,\n        after_kyc_status, after_current_balance, after_account_status\nSTORED AS SCD TYPE 2\nTRACK HISTORY ON * EXCEPT (after_current_balance);\n-- after_current_balance excluded from history tracking (too frequent, not needed)\n-- Result: Full history with __START_AT and __END_AT columns auto-managed\n\n-- ─── Query SCD2 historical table ──────────────\n-- (Auto-generated columns by DLT)\nSELECT\n    after_account_id    AS account_id,\n    after_risk_tier     AS risk_tier,\n    after_kyc_status    AS kyc_status,\n    __START_AT          AS effective_from,\n    __END_AT            AS effective_to,\n    CASE WHEN __END_AT IS NULL THEN 'CURRENT' ELSE 'HISTORICAL' END AS record_status\nFROM LIVE.silver_accounts_scd2\nWHERE after_account_id = 'ACC-EU-98765431'\nORDER BY __START_AT;\n\n-- ─── Gold: Point-in-time risk tier from SCD2 ──\nSELECT\n    txn_id,\n    txn_amount,\n    t.account_id,\n    -- Get risk tier AS OF the transaction date\n    a.after_risk_tier               AS risk_tier_at_txn_time\nFROM eu_bfsi_prod.silver.silver_transactions t\nJOIN eu_bfsi_prod.silver.silver_accounts_scd2 a\n    ON t.account_id = a.after_account_id\n    AND t.txn_timestamp BETWEEN a.__START_AT\n        AND COALESCE(a.__END_AT, TIMESTAMP '9999-12-31 23:59:59')\nWHERE t.txn_date = '2024-06-30';",
    "notes": [
      "APPLY CHANGES INTO is exclusive to DLT — it handles out-of-order events, deduplication, and SCD management automatically",
      "SEQUENCE BY column must be monotonically increasing per key — use Kafka offset, Debezium ts_ms, or database LSN (Log Sequence Number)",
      "SCD TYPE 2 auto-generates __START_AT and __END_AT columns — no manual effective date management required",
      "TRACK HISTORY ON * EXCEPT excludes frequently changing columns (like balances) from SCD2 versioning — prevents version explosion for operational metrics",
      "APPLY CHANGES INTO requires the target table to be a LIVE table (DLT-managed) — it cannot be used with regular Delta tables outside DLT"
    ],
    "use_case": "Real-time Core Banking CDC pipeline at a European challenger bank — Debezium captures every account attribute change from PostgreSQL Core Banking, APPLY CHANGES INTO processes 500K daily CDC events into SCD2, enabling the risk team to query a customer's exact risk classification at any point in time for MiFID II suitability back-testing."
  }
];
